import type { TheoryArticle } from '@/engine/types'

export const electron: TheoryArticle = {
  id: 'th-electron', topic: 'Electron и desktop', title: 'Electron: архитектура desktop-приложения и безопасность IPC',
  lead: 'От Chromium и Node.js до main/renderer/preload, контекстной изоляции, безопасного IPC, обновлений и упаковки приложений.',
  body: `
<h5>Что такое Electron и из чего он собран</h5>
<p>Electron позволяет создавать desktop-приложения web-технологиями. Приложение включает Chromium для рендеринга интерфейса, Node.js runtime для доступа к системным возможностям и Electron API для окон, меню, tray, уведомлений и IPC. Это даёт единый web стек для Windows, macOS и Linux, но поставляет значительную runtime-среду вместе с приложением, поэтому размер дистрибутива, память, обновления и security maintenance становятся частью продукта.</p>
<p>Electron — не «обычный сайт в рамке». У desktop-процесса есть доверенные возможности операционной системы, файловой системы и создания процессов. Главный архитектурный риск — открыть эти полномочия произвольному коду страницы. Поэтому важнейшая граница — отделить интерфейс, который потенциально может содержать XSS, от привилегированных API.</p>

<h5>Main process и renderer process</h5>
<p><b>Main process</b> управляет жизненным циклом приложения, создаёт BrowserWindow, меню, системные диалоги и приложения-интеграции. Он работает в Node.js окружении и имеет широкие полномочия. <b>Renderer process</b> отображает web страницу в окне Chromium; обычно у каждого окна собственный renderer. Renderer должен восприниматься как недоверенный UI-контекст и по умолчанию не должен получать прямой доступ к Node.js.</p>
<p>Процессы изолированы и обмениваются сообщениями, а не общими объектами памяти. Это повышает отказоустойчивость UI, но требует проектировать контракт: типы сообщений, валидацию данных, обработку ошибок, корреляцию запросов и жизненный цикл окна.</p>

<h5>Preload и узкий мост возможностей</h5>
<p><b>Preload script</b> запускается в контексте окна до страницы и может безопасно предоставить ограниченный API через <code class="i">contextBridge</code>. Он является адаптером между web UI и IPC main процесса. Не выставляйте весь <code class="i">ipcRenderer</code>, Node API или универсальную функцию вроде <code class="i">invoke(channel, ...args)</code>: тогда скомпрометированный renderer получает произвольный вызов привилегированных каналов.</p>
<pre class="code">// preload: узко описанные операции
contextBridge.exposeInMainWorld('desktop', {
  chooseExportPath: () =&gt; ipcRenderer.invoke('export:choose-path'),
  saveReport: (input: ReportInput) =&gt;
    ipcRenderer.invoke('report:save', input),
})</pre>
<p>В типах renderer можно объявить интерфейс <code class="i">window.desktop</code>, но compile-time типизация не заменяет runtime-валидацию. Сообщение можно подделать или прислать некорректные поля; main проверяет схему, размер и права пользователя перед действием.</p>

<h5>IPC: invoke, handle и события</h5>
<div data-demo="electron-ipc-lab"></div>
<p>IPC (inter-process communication) — обмен сообщениями между main и renderer. Request/response выполняют через <code class="i">ipcRenderer.invoke</code> и <code class="i">ipcMain.handle</code>: Promise возвращает ответ, а ошибки следует преобразовать в безопасный формат. Односторонние уведомления используют send/on или push через preload. Каналы именуйте предметно и фиксируйте контракты, не позволяя renderer выбирать произвольное имя канала.</p>
<p>В обработчике main проверьте отправителя: известный frame, ожидаемый URL/origin и актуальный window. Контент удалённого origin или неожиданная навигация не должны пользоваться разрешённым desktop API. Ограничьте открытые внешние ссылки системным browser только после проверки протокола и назначения.</p>

<h5>Безопасная конфигурация BrowserWindow</h5>
<ul><li><code class="i">nodeIntegration: false</code> — не внедрять Node.js в страницу renderer.</li><li><code class="i">contextIsolation: true</code> — разделить JS контекст preload и страницы.</li><li><code class="i">sandbox: true</code> — включать sandbox renderer где это совместимо с используемым preload/API.</li><li>Используйте строгую Content Security Policy, уберите небезопасные inline scripts и проверяйте зависимости.</li><li>Запрещайте неожиданные навигации, новые окна и открытие внешних URL без контролируемой политики.</li><li>В production загружайте локальный упакованный UI; избегайте dev URL и удалённого кода с системными привилегиями.</li></ul>
<p>Конкретные значения по умолчанию менялись между версиями Electron. Не полагайтесь на default: задавайте security-critical опции явно и сверяйтесь с документацией версии в package. CSP ограничивает источники JS, CSS, изображений и подключений, но не заменяет sanitization, безопасную маршрутизацию и проверку IPC.</p>

<h5>XSS в desktop опаснее обычной web-страницы</h5>
<p>Если злоумышленник выполнит JavaScript в renderer, безопасная архитектура должна ограничить ущерб несколькими явно выданными действиями. Это defense in depth — несколько независимых барьеров. Не вставляйте пользовательские строки через опасный HTML API без sanitization. Проверяйте переходы, протоколы, deep links, содержимое загруженных файлов и ответы сервера. Не сохраняйте секреты API в renderer или открытой конфигурации: упакованный клиент можно исследовать, а desktop binary нельзя считать доверенным серверным хранилищем.</p>
<p>Приложение с более высоким threat model может использовать permissions policy, allowlist внешних доменов, подпись и проверку обновлений, ограничение доступа к файловым путям и аудит зависимостей. File picker должен возвращать ограниченный результат; путь от renderer не является доверенным только потому, что он локальный.</p>

<h5>Файловая система, диалоги и длинные операции</h5>
<p>Операции файловой системы лучше держать в main: выбрать путь диалогом, проверить расширение/размер, записать во временный файл и атомарно переименовать. При больших файлах используйте потоковую обработку и прогресс, не сериализуйте гигабайты через IPC без необходимости. Операции должны быть отменяемыми или идемпотентными там, где пользователь может повторить действие.</p>

<h5>Жизненный цикл окон, сессии и платформы</h5>
<p>Поведение отличается по ОС: в macOS закрытие последнего окна часто не завершает приложение; на Windows/Linux обычно заканчивает его. Обработайте activate, window-all-closed и повторное создание окна осознанно. Подумайте о single-instance lock, restore окон, deep links, системном tray, файловых ассоциациях и миграциях настроек. Проверяйте различия путей, разрешений, автозапуска и кодовой подписи на каждой целевой платформе.</p>

<h5>Сборка, native modules и обновление</h5>
<p>Electron Forge, electron-builder и другие packaging tools собирают приложение, bundle ресурсов и формат установки. Нативные Node modules должны соответствовать ABI Electron и целевой архитектуре/ОС; это требует rebuild для Electron, а кросс-компиляция может быть ограничена. Для production выпускают подписанные артефакты, notarize macOS пакет по требованиям Apple и отдельно тестируют auto-update каналы.</p>
<p>Обновление — распределённая операция: скачивание, проверка подписи/целостности, установка, перезапуск и миграция локальных данных. Проектируйте совместимость данных с предыдущими версиями и rollback-поведение. Electron регулярно обновляет Chromium и Node; долго не обновлённый runtime может содержать известные уязвимости. Планируйте security releases, а не только функциональные релизы.</p>

<h5>Electron, Tauri и PWA — не взаимозаменяемые ярлыки</h5>
<p>Electron обычно приносит собственный Chromium и Node runtime, что делает web-поведение более унифицированным, но увеличивает размер и ресурсный след. Tauri использует системный webview и отдельный backend слой, поэтому пакет часто меньше, но поведение webview различается по ОС; граница команд и плагины имеют свои security-модели. PWA живёт в браузерном окружении и обычно имеет более ограниченный доступ к ОС, зато проще распространение и обновление. Capacitor упаковывает web UI для мобильных платформ и предоставляет native plugins. Выбор зависит от offline, интеграции с ОС, обновления, размера, платформ и опыта команды.</p>

<h5>Практика: безопасный экспорт отчёта</h5>
<ol><li>Опишите renderer API типом из двух предметных операций: выбрать место и сохранить отчёт.</li><li>Ограничьте формат и размер входа; добавьте schema validation в main.</li><li>Настройте BrowserWindow с отключённым Node integration, включёнными isolation и sandbox, локальным UI и CSP.</li><li>Запретите навигацию на произвольные URL; внешние ссылки отправляйте системному браузеру только для https/mailto после allowlist проверки.</li><li>Добавьте отмену диалога, ошибки прав, безопасную запись и тесты контракта IPC.</li><li>Проверьте XSS threat scenario: какие именно действия останутся возможными из renderer и почему.</li></ol>
<p><b>Проверка собеседования:</b> объясните обязанности main/renderer/preload; почему contextBridge лучше прямого ipcRenderer; что делает contextIsolation; почему типы TypeScript недостаточны для IPC валидации; какие риски даёт remote content и как поддерживать runtime Electron обновлённым.</p>
`,
}
