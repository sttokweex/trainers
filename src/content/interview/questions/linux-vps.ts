import type { Question } from '@/engine/types'

export const linuxVpsQuestions: Question[] = [
  { id:'vps-safe-ssh-change', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'manual', level:'middle', q:'Нужно отключить root login и парольную SSH-аутентификацию. Опишите безопасную последовательность, чтобы не потерять доступ.', solution:'Сначала создать sudo пользователя и добавить ключ, проверить права ~/.ssh, открыть вторую независимую SSH сессию как новый пользователь и проверить sudo; сохранить backup sshd config, сверить include-файлы, изменить одну директиву, sshd -t, reload, открыть третью новую сессию; только после успеха закрыть старые и применить политику.', answer:'<p>Не менять доступ из единственной сессии. Создать deploy/admin пользователя с проверенным sudo и публичным ключом, проверить owner/mode SSH files и войти в новую сессию. Скопировать конфиг, учесть Include-файлы и cloud-init defaults, изменить настройки, выполнить sshd -t, сделать reload и открыть ещё одну новую сессию. Проверить панельную консоль. Только затем отключать прежний путь. При ошибке откатить через живую сессию/out-of-band console.</p>' },
  { id:'vps-apt-update', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'theory', level:'junior', q:'Что именно делает `apt update`, и почему успешное выполнение не означает, что VPS обновлён?', answer:'<p>apt update обновляет локальный индекс доступных пакетов из репозиториев. Он не устанавливает эти версии; для применения используют apt upgrade или целевой upgrade process. Production обновления требуют проверки репозитория/release notes, maintenance window, backup, совместимости и возможного reboot ядра.</p>' },
  { id:'vps-ufw-lockout', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'theory', level:'middle', q:'Почему команда включения UFW может отрезать текущего администратора и какой порядок снижает риск?', answer:'<p>Если политика deny incoming включена до разрешения фактического SSH порта/source, новые и иногда текущие соединения окажутся заблокированы. Сначала определить listener/port и provider firewall, добавить точное allow правило, проверить recovery console, оставить старую сессию, включить firewall и открыть независимую новую сессию. Не тестировать на единственном канале.</p>' },
  { id:'vps-provider-firewall', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'theory', level:'middle', q:'UFW разрешает TCP/443, но сайт извне недоступен. Какие независимые проверки сделать?', answer:'<p>Проверить cloud firewall/security group provider-а, public IP и DNS A/AAAA, listener через ss, Nginx статус и bind, host firewall v4/v6, route и фактический packet path. Пакет должен пройти все фильтры; локальный curl не доказывает внешний reachability.</p>' },
  { id:'vps-systemd-path', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'manual', level:'middle', q:'Node приложение работает в интерактивном shell, но падает под systemd с `203/EXEC` или `node: not found`. Что исследовать?', solution:'Проверить systemd status/journal, абсолютный ExecStart (command -v node в корректном окружении), права executable и parent directories, WorkingDirectory, User, EnvironmentFile и отсутствие зависимости от интерактивного shell/nvm profile. Затем systemd-analyze verify, daemon-reload и controlled restart.', answer:'<p>systemd не обязан загружать shell profile и nvm environment. Указать абсолютный путь Node/runtime или управляемый deployment path, корректный ExecStart, существующий executable, права пользователя и WorkingDirectory. Посмотреть journalctl -u unit и systemctl show. Проверить EnvironmentFile и пути. После исправления systemd-analyze verify, daemon-reload, restart и контроль status/health.</p>' },
  { id:'vps-systemd-sigterm', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'theory', level:'senior', q:'Что должно делать web-приложение при SIGTERM и почему мгновенное завершение опасно при деплое?', answer:'<p>Прекратить принимать новые запросы, начать draining существующих в пределах deadline, завершить/отложить фоновые задачи корректно, закрыть DB/queue connections и выйти. Немедленный exit может оборвать responses или потерять полученную задачу. Нужны согласованные systemd stop timeout, proxy draining и идемпотентные worker semantics.</p>' },
  { id:'vps-nginx-502', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'manual', level:'middle', q:'После релиза Nginx отдаёт 502. Составьте порядок локализации и объясните, почему не стоит сразу открывать Node порт наружу.', solution:'Проверить nginx error log/upstream failure, nginx -t/status, service status/journal, listener ss и адрес/порт proxy_pass; локально curl к upstream; проверить app readiness, crash/config/DB; проверить protocol mismatch, bind и rollout. Сохранить private listener и исправить proxy path, не открывать лишнюю поверхность наружу.', answer:'<p>502 создаётся proxy, когда upstream недоступен или ответ некорректен. Сначала error.log для конкретной причины (connection refused, timeout, reset), затем app unit/logs, listener и соответствие proxy_pass host/port/protocol. curl к 127.0.0.1 upstream отделяет Nginx от приложения. Проверить готовность и зависимости. Открытие backend наружу маскирует ошибку архитектуры и расширяет attack surface; оставь API на loopback/private network.</p>' },
  { id:'vps-disk-inodes', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'theory', level:'middle', q:'`df -h` показывает свободные гигабайты, но приложения не могут создать файлы. Как проверить inode exhaustion и удалённый открытый лог?', answer:'<p>Выполнить df -ih для свободных inodes. Найти большое количество мелких файлов и их владельца; для байтового usage использовать du. Удалённый файл может оставаться открытым процессом и занимать blocks: lsof +L1. Не удалять случайные runtime/database файлы, определить retention и безопасный способ log rotation/reopen.</p>' },
  { id:'vps-load-average', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'theory', level:'middle', q:'На VPS load average выше числа vCPU, но CPU utilization не очень высокий. Какие объяснения проверить?', answer:'<p>Linux load average включает runnable и uninterruptible задачи, часто ожидающие дисковый I/O, а не только задачи, реально использующие CPU. Проверить top/iostat/vmstat, blocked tasks, storage latency, memory pressure/swap и процессы. Сопоставить интервалы и vCPU; load average не равен проценту CPU и сам по себе не доказывает нехватку вычислений.</p>' },
  { id:'vps-cert-renewal', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'theory', level:'middle', q:'Сертификат выпущен вручную, но сайт через 90 дней стал выдавать expiry error. Как построить проверяемое автообновление?', answer:'<p>Проверить certbot installer, renewal config и systemd timer/cron, запустить certbot renew --dry-run, убедиться, что challenge-пути и DNS/ports доступны. Проверить reload hook Nginx и сертификат после обновления, добавить монитор срока и тест восстановления приватного ключа. Вручную полученный сертификат без renewal process не считается настроенным.</p>' },
  { id:'vps-backup-rto-rpo', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'theory', level:'middle', q:'Чем RPO отличается от RTO и почему snapshot VPS может быть недостаточным backup для базы?', answer:'<p>RPO — допустимая потеря данных во времени; RTO — приемлемое время восстановления. Snapshot может быть crash-consistent, захватить незавершённое состояние БД, находиться в том же failure domain и не проверять restore. Нужны согласованный DB backup/WAL стратегия, отдельное хранение, доступные ключи и регулярное пробное восстановление.</p>' },
  { id:'vps-ssh-vs-sudo', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'theory', level:'junior', q:'Зачем повседневно работать через отдельного sudo пользователя вместо постоянного root shell?', answer:'<p>Снижает риск случайных разрушительных команд и даёт аудит, кто запросил privilege escalation. Root нужен для ограниченных административных операций, а приложение должно запускаться отдельным service user с минимальными правами. Это не отменяет необходимость ограничить SSH, логировать sudo и защищать учетную запись.</p>' },
  { id:'vps-docker-localhost', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'theory', level:'junior', q:'Приложение в Docker контейнере слушает 127.0.0.1:3000 внутри контейнера и недоступно через опубликованный порт. Почему?', answer:'<p>Loopback контейнера доступен только внутри его network namespace. Для доступа через container networking приложение обычно должно слушать 0.0.0.0 внутри контейнера; затем published port и host/provider firewall должны совпадать. Не следует публиковать порт в интернет, если достаточно внутренней Docker network и reverse proxy.</p>' },
  { id:'vps-release-rollback', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'manual', level:'senior', q:'Спроектируйте небольшой безопасный deploy на одну VPS с откатом приложения, учитывая миграцию базы и завершение активных запросов.', solution:'CI собирает immutable artifact; копирует в новый versioned release; проверяет конфиг, совместимую миграцию expand-only и smoke check на непубличном порту; переключает current symlink атомарно; Nginx продолжает proxy; systemd graceful restart/drain; проверяет health/logs; rollback symlink/code возможен пока schema backward-compatible; cleanup старых releases по retention.', answer:'<p>CI публикует immutable artifact. Сервер кладёт его в отдельный versioned release, выставляет ownership, валидирует config и запускает health check. Миграция сначала backward-compatible (expand/dual-compatible), destructive contraction позже. Атомарно переключить current symlink и выполнить graceful restart с SIGTERM drain; проверить health, ошибки и метрики. Rollback к прошлому release возможен, пока схема совместима. Хранить предыдущие версии и backup БД, не редактировать live файлы вручную.</p>' },
  { id:'vps-firewall-checklist', theoryId:'th-linux-vps', topic:'Linux и VPS', type:'manual', level:'middle', q:'Составьте правило firewall для публичного Node API за Nginx: что должно быть открыто снаружи и что обязано остаться закрытым?', solution:'Внешний ingress только необходимые 80/443 и администрирование SSH по ограниченному source/VPN; Node listener только loopback или private interface, DB/Redis только private network и нужному source. Настроить обе панели provider и host firewall, IPv4 и IPv6; проверить с внешнего хоста и через ss; задокументировать recovery.', answer:'<p>Публично нужны HTTP/HTTPS для Nginx и ограниченный админский SSH (лучше VPN/allowlisted source), если нужен. Node должен слушать localhost/private network и быть недоступным напрямую; БД/Redis не публиковать. Разрешения настроить в provider firewall и UFW/nftables, отдельно учесть IPv6. Проверить port scan/connection из внешней сети и локальный upstream, записать recovery путь.</p>' },
]
