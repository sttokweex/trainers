import type { TheoryArticle } from '@/engine/types'

export const threejs: TheoryArticle = {
  id: 'th-threejs', topic: 'Three.js', title: 'Three.js и 3D в браузере: сцена, GPU, материалы и производительность',
  lead: 'Учебный маршрут от системы координат и графа сцены до glTF, raycasting, WebGL/WebGPU и production-ограничений интерактивной 3D-графики.',
  body: `
<h5>Что такое Three.js и что он скрывает</h5>
<p>Three.js — библиотека JavaScript для построения 3D-сцен и отправки их на графический API браузера. Обычно приложение описывает сцену объектами JavaScript, а библиотека формирует команды для WebGL renderer; в актуальной экосистеме развивается и WebGPU renderer, возможности и совместимость которого зависят от версии и браузера. Three.js не является игровым движком со встроенной физикой, сетью и редактором уровня: для этого подключают отдельные библиотеки или пишут собственную логику.</p>
<p>GPU — графический процессор, который параллельно обрабатывает вершины и фрагменты. Canvas/WebGL не отменяет основную нить: загрузка модели, построение scene graph и часть подготовки всё равно могут работать на CPU. Three.js абстрагирует низкоуровневые буферы и шейдеры, но чтобы оптимизировать страницу, нужно понимать draw calls, геометрию, текстуры, память GPU и частоту кадров.</p>

<h5>Сцена, камера, геометрия, материал, renderer</h5>
<p><b>Scene</b> — корневой контейнер объектов и освещения. <b>Camera</b> задаёт точку зрения и проекцию; perspective camera имитирует перспективу, orthographic camera сохраняет одинаковый размер объектов независимо от глубины. <b>Geometry</b> содержит вершины, индексы и атрибуты. <b>Material</b> описывает реакцию поверхности на свет или шейдер. <b>Renderer</b> связывает сцену и камеру с canvas и создаёт GPU команды.</p>
<pre class="code">const scene = new THREE.Scene()
const camera = new THREE.PerspectiveCamera(50, width / height, 0.1, 100)
camera.position.set(0, 1, 5)
const renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true })
renderer.setSize(width, height)
container.append(renderer.domElement)
scene.add(new THREE.Mesh(geometry, material))
renderer.render(scene, camera)</pre>
<p>Near/far clipping planes задают диапазон глубины камеры; слишком далёкий far и слишком близкий near снижают точность depth buffer и вызывают z-fighting — мерцание почти совпадающих поверхностей. Подбирайте разумный диапазон, а не ставьте near почти в ноль и far в миллионы без причины.</p>

<h5>Координаты, transform и scene graph</h5>
<p>Three.js по умолчанию использует правостороннюю систему координат: x вправо, y вверх, z к наблюдателю (конвенция камеры важнее запоминания «куда» без контекста). Объект имеет position, rotation и scale. Локальный transform дочернего объекта умножается на world transform родителя. Scene graph — дерево этих отношений; оно позволяет двигать сложный объект одним родителем и получать мировую матрицу дочерних mesh.</p>
<p>Порядок вращений важен: Euler rotation зависит от порядка осей и может столкнуться с gimbal lock; quaternion кодирует ориентацию без этой сингулярности и удобен для интерполяции. Единицы приложения условны, но полезно договориться, что одна единица соответствует, например, метру. Это упрощает свет, физику, камеру и экспорт.</p>

<h5>Render loop, delta time и анимация</h5>
<p>Для непрерывной сцены используют <code class="i">requestAnimationFrame</code>. Позицию следует менять через delta time — время между кадрами в секундах, чтобы скорость не зависела напрямую от частоты дисплея. Для анимации по абсолютному времени можно использовать timestamp. На скрытой вкладке браузер обычно замедляет/приостанавливает кадры, поэтому после возврата ограничивайте чрезмерно большой delta.</p>
<pre class="code">const clock = new THREE.Clock()
function frame() {
  requestAnimationFrame(frame)
  const delta = Math.min(clock.getDelta(), 0.05)
  mesh.rotation.y += delta * 0.7
  renderer.render(scene, camera)
}
frame()</pre>
<p>Не делайте каждый кадр тяжёлые вычисления, новый DOM, аллокации больших массивов или пересоздание материала. Для статичной сцены можно рендерить по событию изменения или использовать demand rendering: это экономит батарею и освобождает GPU.</p>

<h5>Свет, PBR, tone mapping и цвет</h5>
<p>MeshStandardMaterial/PhysicalMaterial используют PBR (physically based rendering) модель: roughness описывает микрошероховатость, metalness — металлический характер поверхности. Реалистичный результат требует корректного освещения и environment map, а не только «поставить metalness в 1». Интенсивности света, color space входных текстур и tone mapping влияют на итоговые цвета. Ошибки sRGB/linear могут давать неожиданно тёмное или выцветшее изображение.</p>
<p>Тени дороги: shadow map требует отдельной работы и дополнительной памяти, а слишком большие shadow camera bounds снижают разрешение. Включайте тени только где они улучшают понимание формы; для статичного объекта baked lighting/environment map может быть дешевле.</p>

<h5>Raycasting: выбор объектов мышью и касанием</h5>
<p>Raycasting выпускает математический луч из камеры через точку экрана и ищет пересечения с объектами. Координаты мыши переводят в normalized device coordinates диапазона -1..1 с учётом bounding rectangle canvas, затем обновляют raycaster. Для больших сцен наивный перебор объектов дорог: ограничивайте список selectable, используйте bounding volumes, spatial index или BVH. На мобильном поддержите pointer events и разумный размер целей касания.</p>
<p>Пересечение возвращает расстояние, точку, объект и иногда грань/UV; это даёт возможность показать подсказку, выбрать секцию или отобразить texture annotation. Нужно решить, что происходит с прозрачными/невидимыми слоями, перекрытиями, controls камеры и accessibility DOM fallback для ключевых действий.</p>

<h5>Модели glTF/GLB и управление ресурсами</h5>
<p>glTF — переносимый формат сцен, материалов, анимаций, скининга и текстур; GLB — его бинарная упаковка. glTF широко поддерживается WebGL-инструментами и часто подходит для web-доставки. Загрузчик асинхронен: показывайте progress/failure, отменяйте устаревшие запросы и учитывайте, что файл модели — это не только геометрия, но и текстуры/дополнительные ресурсы. Для подготовки применяют оптимизацию геометрии, сжатие текстур и mesh/texture compression, проверяя decoder setup и целевые устройства.</p>
<p>GPU ресурсы не всегда освобождаются сборщиком мусора автоматически. При удалении объекта нужно явно dispose-ить геометрии, материалы и текстуры, если они больше не используются; shared ресурсы нельзя уничтожить, пока ими пользуется другая сцена. При размонтировании React/Vue компонента отменяйте RAF, удаляйте listeners, controls и renderer context при необходимости.</p>

<h5>Размер canvas, DPR и адаптивность</h5>
<p>Размер canvas в CSS pixels отличается от backing buffer. Умножение на devicePixelRatio делает изображение резче на Retina, но площадь пикселей растёт квадратично: DPR=3 требует примерно в девять раз больше пикселей, чем DPR=1. Ограничивайте DPR, например верхним пределом, и измеряйте производительность на слабых телефонах. Используйте ResizeObserver или наблюдение за контейнером; обновляйте aspect camera и renderer size при смене layout.</p>
<p>Canvas сам по себе не раскрывает смысл 3D сцены скринридеру. Предусмотрите DOM-подписи, альтернативный список selectable объектов, управление с клавиатуры, инструкции для reduced motion и статическое fallback-изображение при невозможности создать WebGL контекст.</p>

<h5>Производительность: что измерять и оптимизировать</h5>
<ul><li>Draw calls: группируйте совместимые материалы и геометрию, используйте instancing для повторяющихся объектов.</li><li>Треугольники и vertices: удаляйте невидимую детализацию, используйте LOD для расстояния.</li><li>Texture memory: контролируйте размеры и форматы текстур; компрессия влияет на decode и загрузчики.</li><li>Overdraw/fill rate: прозрачные крупные поверхности и постобработка повышают стоимость пикселей.</li><li>CPU: профилируйте scene traversal, загрузку, GC и обработчики событий.</li><li>GPU frame time: измеряйте на реальных устройствах, а не только FPS на мощном desktop.</li></ul>
<p>Workers и OffscreenCanvas могут переносить часть работы, но поддержка, ограничения контекста и архитектурная сложность зависят от браузера и библиотечной версии. Утверждение «весь Three.js легко вынести в worker» неверно: проверьте реально используемый renderer и загрузчики.</p>

<h5>Интеграция в SPA и маршруты</h5>
<p>Тяжёлый 3D editor часто загружают по требованию через lazy route, чтобы базовый экран не оплачивал его JS и текстуры. В React/Vue lifecycle создавайте renderer после mount и освобождайте ресурсы при unmount. SSR не может исполнить WebGL DOM-код на сервере; используйте client-only boundary или placeholder. Избегайте hydration mismatch, если сервер выдал другую разметку, чем первый клиентский render.</p>

<h5>Практика: вращаемая модель товара</h5>
<ol><li>Создайте сцену с orthographic и perspective camera и сравните проекцию.</li><li>Подгрузите оптимизированную GLB модель, покажите loading, retry и ошибку.</li><li>Реализуйте orbit controls, запретите чрезмерный zoom и добавьте клавиатурный способ поворота.</li><li>Сделайте raycast по hotspot и синхронный DOM список этих точек.</li><li>Измените ширину контейнера и DPR; сравните время кадра и память на мобильном.</li><li>Размонтируйте сцену и докажите, что остановлены RAF и listeners и освобождены уникальные GPU ресурсы.</li></ol>
<p><b>Проверка собеседования:</b> почему delta time важен; чем geometry отличается от material; почему высокий DPR может снизить FPS; как устроен raycasting; что надо dispose-ить; зачем сцена с canvas нуждается в DOM-доступной альтернативе.</p>
`,
}
