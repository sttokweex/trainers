import type { TheoryArticle } from '@/engine/types'

export const ssr: TheoryArticle = { id:'th-ssr', topic:'React', title:'SSR, SSG, streaming и гидрация',
  lead:'Как серверный HTML помогает первому отображению и SEO, что делает гидрация и какие проблемы появляются в изоморфном приложении.',
  body:`
<h5>CSR: рендер только в браузере</h5>
<p>При <b>Client-Side Rendering (CSR)</b> сервер обычно отдаёт HTML-оболочку и JavaScript. Браузер загружает JS, исполняет приложение, получает данные и строит интерфейс. Это удобно для интерактивности и простой инфраструктуры, но пользователь может долго видеть пустую оболочку; поисковику и assistive technology иногда приходится ждать JS. Время до интерактивности зависит от загрузки, парсинга и работы main thread.</p>

<h5>SSR и SSG</h5>
<p><b>Server-Side Rendering (SSR)</b> формирует HTML на сервере для запроса пользователя. Браузер может раньше показать содержимое, а затем загрузить JS и подключить обработчики. Персонализированный HTML требует учитывать кеширование, cookies, авторизацию и нагрузку на сервер.</p>
<p><b>Static Site Generation (SSG)</b> формирует HTML при build для заранее известных маршрутов. CDN отдаёт статические артефакты быстро, но часто меняющиеся данные требуют повторной сборки или отдельной стратегии обновления. <b>Incremental/ISR-подобные модели</b> обновляют часть статических страниц по времени или событию; конкретные semantics зависят от framework.</p>
<p><b>Streaming SSR</b> отправляет готовые фрагменты HTML по мере готовности, иногда используя Suspense boundaries. Это может ускорить отображение независимых частей, но усложняет обработку ошибок, состояние заголовков, кеш и порядок скриптов. Нельзя считать, что любая страница автоматически становится быстрее от включения streaming.</p>

<h5>Что такое гидрация</h5>
<p><b>Гидрация (hydration)</b> — браузер загружает клиентский код и связывает его с HTML, который уже отрендерил сервер: добавляет обработчики и восстанавливает дерево компонентов без полной замены разметки. Серверный HTML может быть виден до завершения гидрации, но кнопки до подключения обработчиков могут быть неинтерактивны. Это окно называют периодом «видно, но ещё не интерактивно».</p>
<p>React ожидает, что клиентский первый render совпадает с серверной разметкой. Если данные или условия различаются, возникает hydration mismatch: лишняя работа, предупреждение или локальное/полное пересоздание subtree. Скрывать предупреждение без выяснения источника — не исправление.</p>

<h5>Частые причины mismatch</h5>
<ul><li>Нестабильные значения во время render: <code class="i">Date.now()</code>, случайное число, uuid без общего seed.</li><li>Разные timezone/locale или формат даты на Node и браузере.</li><li>Чтение <code class="i">window</code>, localStorage или viewport при серверном render; на сервере этих browser API нет.</li><li>Пользовательские данные/feature flags на сервере и клиенте не совпали.</li><li>Неправильная HTML-вложенность: браузерный HTML parser автоматически исправляет DOM, и фактическая структура отличается от React tree.</li><li>CSS-in-JS генерирует другой порядок/идентификаторы стилей.</li><li>Расширение браузера или сторонний скрипт изменил DOM до гидрации.</li></ul>
<p>Используйте одинаковые сериализованные initial data, детерминированный render и browser-only эффекты после монтирования. Для значений, намеренно меняющихся на клиенте, спроектируйте отдельный boundary/placeholder. <code class="i">suppressHydrationWarning</code> — точечный escape hatch для ожидаемого расхождения, а не универсальный способ скрыть баг.</p>

<h5>Граница сервера и клиента</h5>
<p>Серверный компонент/модуль не обязательно становится частью клиентского bundle; интерактивный участок может требовать client component и передаваемые сериализуемые props. Server/client boundary влияет на то, какие модули, секреты и данные попадут в браузер. Серверные секреты должны оставаться в server-only коде, а сериализованный HTML/props могут быть публично видимы пользователю.</p>
<p>SSR не отменяет авторизацию: сервер проверяет доступ на каждый запрос. Кеширование пользовательского HTML опасно, если ключ кеша не учитывает пользователя/locale/tenant. Персональные ответы обычно приватные или кешируются по безопасно определённым ключам; не полагайтесь на то, что CDN «сам поймёт», какой контент персонализирован.</p>

<h5>Производительность: что сравнивать</h5>
<p>SSR может улучшить time-to-first-contentful paint, но добавить серверный latency и стоимость вычисления. Большой hydration bundle всё ещё блокирует интерактивность. Сравнивайте TTFB, FCP/LCP, INP, время до интерактивного поведения, число JS байт и серверную стоимость на реальных устройствах/сети. Server rendering может помочь поисковой индексации, но современные поисковые системы умеют выполнять JS; конкретная потребность зависит от аудитории и сценария.</p>
<p>Partial hydration/islands отправляет JS только отдельным интерактивным островкам, progressive enhancement делает базовые формы работающими без полного JS. Эти методы снижают ненужную клиентскую работу, но требуют архитектурно ограничить интерактивность.</p>

<h5>Ошибки, кеш и эксплуатация</h5>
<p>Серверный render может завершиться ошибкой до отправки HTML, а клиентская гидрация — упасть уже после ответа. Нужны error boundaries на клиенте, server logs/traces, correlation id, стратегия fallback и защита от утечки stack trace. Кешируйте только воспроизводимый контент с верным cache key; серверный render не должен случайно использовать данные прошлого пользователя через общий mutable singleton.</p>
<p>При выкладке согласуйте HTML и assets той же версии. Старый HTML может ссылаться на удалённый JS. Хэшированные файлы сохраняйте для живых вкладок, а HTML/manifest переключайте атомарно.</p>

<h5>Как выбрать rendering strategy</h5>
<table><tr><th>Стратегия</th><th>Подходит</th><th>Основная цена</th></tr>
<tr><td>CSR</td><td>внутренние панели, приложения после входа, сложная интерактивность</td><td>поздний первый контент и крупная работа клиента</td></tr>
<tr><td>SSR</td><td>персональная или свежая страница с ценностью раннего HTML</td><td>серверная нагрузка, кеширование, границы сервера/клиента</td></tr>
<tr><td>SSG</td><td>контент редко меняется и маршруты известны заранее</td><td>обновление/инвалидация страниц и сборка</td></tr>
<tr><td>Streaming / islands</td><td>нужно показать независимые части рано и сократить ненужный JS</td><td>сложнее модель ошибок, гидрации и наблюдаемости</td></tr></table>

<h5>Как отвечать на собеседовании</h5>
<p>Определите стратегию и отдельно проговорите доставку HTML, данных и клиентского JS. Объясните hydration mismatch, browser-only API и сериализацию; сравните TTFB с LCP/INP; назовите кеш-ключ и риск персональных данных; затем выберите SSR, SSG или CSR под конкретный сценарий. «SSR быстрее» — неполный ответ без этих компромиссов.</p>` }
