import type { TheoryArticle } from '@/engine/types'

export const build: TheoryArticle = { id:'th-build', topic:'Браузер', title:'Сборка фронтенда: Vite, Webpack и бандлы',
  lead:'Как исходный код превращается в файлы для браузера: граф модулей, dev-сервер, плагины, чанки, CSS, кеш и диагностика типичных проблем.',
  body:`
<h5>1. Что именно делает сборка</h5>
<p><b>Сборка</b> — подготовка исходников проекта к запуску в целевой среде. Для браузерного приложения инструменты разрешают импорты, преобразуют синтаксис и ресурсы, связывают модули, делят результат на файлы и могут минифицировать его. Сборщик не «ускоряет приложение магически»: он создаёт артефакты, которые ещё нужно правильно загрузить, закешировать и выполнить.</p>
<p><b>Модуль</b> — файл или единица кода с импортами и экспортами. <b>Граф модулей</b> — ориентированная схема зависимостей: от entry point (точки входа) можно пройти ко всем нужным модулям. Изменение одного узла может потребовать пересчитать его самого и часть графа, но не обязательно весь проект.</p>
<p>Сборка обычно состоит из нескольких стадий:</p>
<ol><li><b>Разрешение (resolve)</b>: найти файл для <code class="i">import './Button'</code>, учесть расширения, aliases, package exports и условия среды.</li><li><b>Преобразование (transform)</b>: TS/JSX, CSS Modules, Sass, изображения и другие входы превратить в код, понятный следующей стадии.</li><li><b>Связывание (bundling)</b>: собрать связанные модули в один или несколько выходных файлов, если выбран такой режим.</li><li><b>Оптимизация</b>: удалить доказуемо неиспользуемый код, минифицировать, разделить чанки и переписать ссылки на ресурсы.</li><li><b>Выдача артефакта</b>: HTML, JS, CSS, картинки, шрифты и карту исходников для отладки.</li></ol>
<p><b>Бандл</b> — выходной файл или набор связанных файлов после сборки. <b>Чанк</b> — отдельная часть такого результата, которую можно загрузить независимо. Поэтому «размер бандла» может означать размер одного файла, суммарный размер всех файлов или объём, который реально загрузился на первом экране — это разные метрики.</p>

<h5>2. ESM, CommonJS и почему важен формат модулей</h5>
<table><tr><th></th><th>ESM</th><th>CommonJS</th></tr>
<tr><td>Синтаксис</td><td><code class="i">import</code> / <code class="i">export</code></td><td><code class="i">require</code> / <code class="i">module.exports</code></td></tr>
<tr><td>Связи</td><td>в основном статически видны анализатору</td><td>могут вычисляться во время выполнения</td></tr>
<tr><td>Экспорт</td><td>живые привязки к экспортируемым значениям</td><td>свойства объекта экспорта</td></tr>
<tr><td>Загрузка в браузере</td><td>поддерживается нативно</td><td>напрямую браузером не поддерживается</td></tr>
<tr><td>Tree-shaking</td><td>обычно анализировать проще</td><td>возможности ограничены, зависят от формы кода</td></tr></table>
<p>ESM даёт сборщику хорошие подсказки, но сам факт использования <code class="i">import</code> не гарантирует удаление неиспользуемого кода. Динамический <code class="i">import()</code> возвращает Promise и позволяет загрузить модуль позже; конкретное разбиение выходных файлов определяет сборщик и его конфигурация.</p>
<div class="hint">Не отвечайте, что CommonJS всегда синхронен, а ESM всегда асинхронен в любом смысле. Синтаксис ESM статически связывается и браузерная загрузка модулей встроенно асинхронна; внутри уже загруженного графа выполнение имеет правила порядка зависимостей. В Node.js детали загрузки зависят от режима и версии.</div>

<h5>3. Vite: dev-сервер и pre-bundling</h5>
<p>Vite разделяет <b>разработку</b> и <b>production-сборку</b>. В dev-режиме сервер не обязан сначала упаковать всё приложение в один бандл. Он отдаёт HTML, браузер запрашивает ESM-модули, а Vite преобразует запросы на лету и обслуживает их. Поэтому старт часто быстрый даже при большом графе исходников: работа над исходниками распределена по запросам и выполняется по мере необходимости.</p>
<p>Это не означает «Vite вообще ничего не собирает» или «каждый исходный файл без изменений идёт браузеру». JSX, TypeScript-синтаксис, импорты CSS, алиасы и виртуальные модули требуют обработки. Dev-сервер может сканировать граф, хранить кэш, предобрабатывать зависимости и повторно преобразовывать файлы после изменений.</p>
<h6>Оптимизация зависимостей</h6>
<p><b>Dependency pre-bundling</b> — предварительная обработка зависимостей из <code class="i">node_modules</code> для dev-режима. Она помогает привести CommonJS/UMD-пакеты к удобному для браузера ESM-виду и сократить число запросов для пакетов с множеством внутренних файлов. Это прежде всего ускорение разработки, а не production-оптимизация. Кэш находится в каталоге Vite и может пересоздаваться после изменения lock-файла, конфигурации или набора зависимостей.</p>
<p>Фразы «Vite использует esbuild» и «Vite использует Rollup» верны только с уточнением версии и стадии. В Vite 5 production-сборка основана на Rollup; версии Vite 6/7 обновляли инструменты преобразования и сборки, а направление развития production-бандлера меняется. На собеседовании называйте версию проекта и смотрите актуальную документацию: публичная модель Vite важнее запоминания внутренней реализации одного релиза.</p>

<h5>4. Vite: production build</h5>
<p>В production Vite строит оптимизированный набор файлов: извлекает CSS, обрабатывает ассеты, формирует чанки для статических и динамических импортов, применяет tree-shaking и минификацию в соответствии с конфигурацией. Эти файлы затем отдаёт веб-сервер или CDN. Dev-сервер Vite не является production-сервером.</p>
<pre class="code"># типичный цикл
pnpm dev       # dev-сервер и HMR
pnpm build     # проверка типов (если настроена) и production-артефакты
pnpm preview   # локально проверить собранный результат</pre>
<p>Проверяйте реальный production build: dev-режим не покажет проблемы с базовым путём приложения, регистрами имён файлов на Linux, отсутствующими env-переменными, заголовками кеша и размещением за подкаталогом. <code class="i">base</code> в конфигурации задаёт публичный базовый путь, например для размещения сайта не в корне домена.</p>

<h5>5. Webpack: от графа к chunks</h5>
<p>Webpack строит граф зависимостей от одной или нескольких точек входа и создаёт бандлы/чанки. Его модель расширяется через <b>loaders</b> и <b>plugins</b>. Webpack работает и в dev, и в production, но режим разработки можно настроить на быструю пересборку, memory filesystem и HMR; не следует представлять его как обязательную полную production-сборку при каждом запросе.</p>
<ul><li><b>Loader</b> преобразует содержимое отдельного типа ресурса или файла в модуль: например TypeScript, CSS, SVG. Для одного файла правила могут применяться цепочкой.</li><li><b>Plugin</b> подключается к жизненному циклу компилятора и решает более широкие задачи: HTML, manifest, анализ, копирование ресурсов, извлечение CSS. Плагины могут менять граф и артефакты.</li><li><b>Entry</b> задаёт точку входа; <b>output</b> — формат и путь результатов; <b>resolve</b> — правила поиска модулей.</li><li><b>Mode</b> влияет на оптимизации и встроенные значения, но не заменяет осмысленную конфигурацию и проверку результата.</li></ul>
<p>У Webpack гибкая и зрелая экосистема, много настроек и интеграций, включая Module Federation. Цена — сложность конфигурации и необходимость понимать взаимодействие правил, плагинов и оптимизаций. Сравнение «Webpack медленный, Vite быстрый» — слишком грубое: время зависит от размера проекта, кэша, плагинов, loader-ов, машины, типа изменений и выбранного режима.</p>

<h5>6. HMR и обновление без перезагрузки</h5>
<p><b>HMR</b> (Hot Module Replacement) заменяет изменившийся модуль в запущенном приложении без полной перезагрузки страницы, когда цепочка модулей умеет принять обновление. Если границы HMR нет или состояние нельзя безопасно сохранить, инструмент может сделать полную перезагрузку. HMR — удобство разработки, не механизм production-доставки.</p>
<p>HMR не гарантирует сохранение любого состояния. Например, изменение структуры React-компонента может сбросить локальное состояние; глобальные побочные эффекты могут выполниться повторно. При «странном» состоянии после правки полезно сделать полную перезагрузку и проверить, воспроизводится ли дефект в чистом запуске.</p>

<h5>7. Tree-shaking, side effects и почему библиотека остаётся в бандле</h5>
<p><b>Tree-shaking</b> — удаление неиспользуемых экспортов и связанного с ними недостижимого кода. Это оптимизация по анализу графа, а не удаление строк, которые разработчик субъективно считает ненужными. Сборщик должен доказать, что код не нужен и его удаление не изменит наблюдаемое поведение.</p>
<ul><li>Статические ESM-импорты анализируются лучше вычисляемых загрузок и CommonJS-обёрток.</li><li>Верхнеуровневый код модуля может иметь <b>side effect</b> — побочный эффект: регистрацию обработчика, полифил, изменение глобального состояния. Такой импорт нельзя просто выбросить.</li><li><code class="i">"sideEffects": false</code> в <code class="i">package.json</code> — обещание авторов пакета, что модули можно безопасно исключать при соответствующих условиях. Если пометить так CSS или модуль с регистрацией, можно сломать приложение.</li><li>Разные форматы, условия экспорта пакета и development/production ветки могут приводить к тому, что бандлер видит не тот entry point.</li></ul>
<p>Named import сам по себе не значит «в бандле будет только одна функция»: важны формат пакета, побочные эффекты, barrel-файлы (файлы-агрегаторы экспортов), конфигурация и фактическая достижимость кода.</p>

<h5>8. Code splitting и стратегия чанков</h5>
<p><b>Code splitting</b> делит код на загружаемые части. Разделение возникает из точек входа, динамических импортов и правил сборщика. Практический выигрыш — не минимальный размер всех файлов суммарно, а меньший объём критического JS для конкретного сценария при приемлемом числе запросов и накладных расходов.</p>
<pre class="code">// React: маршрут можно загрузить отдельно
const ReportsPage = React.lazy(() =&gt; import('./pages/ReportsPage'))

// редкая функция — тяжёлая зависимость нужна только при действии пользователя
async function exportReport(data) {
  const { exportToCsv } = await import('./export-to-csv')
  return exportToCsv(data)
}</pre>
<p><b>Chunk</b> (чанк) — файл или группа модулей, сформированная для загрузки. Если два маршрута используют одну тяжёлую библиотеку, сборщик может вынести общий код в shared chunk. Неудачные правила ручного разделения способны создать слишком много мелких файлов, повторить код или породить циклические зависимости между чанками.</p>
<p>Не выносите все зависимости в один «vendor.js» автоматически. Если меняется один пакет, общий файл может инвалидировать долгий кеш; если разделить каждый пакет на отдельный файл, вырастут запросы и runtime-накладные расходы. Выбирайте границы по графу, частоте изменений и реальным сценариям загрузки, затем анализируйте результат.</p>
<p>Предзагрузка также различается: <b>preload</b> нужен текущей странице и загружается с высоким приоритетом; <b>prefetch</b> — вероятному будущему сценарию с низким приоритетом. Избыточный preload конкурирует за сеть с критическим CSS, шрифтом и главным JS.</p>

<h5>9. CSS, HTML и статические ресурсы в сборке</h5>
<p>Импорт CSS из JS связывает таблицу стилей с графом модулей. В dev стили часто обновляются отдельно через HMR; в production CSS может извлекаться в отдельные файлы, минифицироваться и связываться с чанками. Важно проверить порядок стилей: каскад, специфичность и порядок импорта сохраняют значение и после сборки.</p>
<ul><li><b>CSS Modules</b> локализуют имена классов в конкретном модуле, но не изолируют глобальные селекторы, переменные и наследование полностью.</li><li><b>CSS-in-JS</b> может генерировать CSS во время выполнения или на этапе сборки — стратегия зависит от библиотеки и влияет на SSR, размер и порядок стилей.</li><li>Небольшая картинка может встраиваться как data URL, большая — выпускаться отдельным файлом; порог и поведение зависят от инструмента и настроек.</li><li>Шрифты, SVG и изображения участвуют в графе только при корректном импорте/ссылке. Проверьте URL после деплоя в подкаталог.</li><li>HTML часто создаётся плагином с хэшированными ссылками на артефакты. Старый HTML и новые чанки могут смешаться при некорректном деплое.</li></ul>

<h5>10. Переменные окружения и секреты</h5>
<p>Клиентская сборка обычно подставляет значения окружения <b>во время сборки</b>. После публикации изменение переменной на сервере не изменит уже созданный JS, если приложение специально не читает runtime-конфиг. В Vite клиентские переменные с префиксом <code class="i">VITE_</code> доступны через <code class="i">import.meta.env</code>; в других системах префикс отличается.</p>
<pre class="code">const apiUrl = import.meta.env.VITE_API_URL
if (import.meta.env.DEV) {
  console.info('dev-only diagnostics')
}</pre>
<div class="key">Любое значение, встроенное в клиентский бандл, публично: его можно найти в JS, source map или сетевых запросах. API URL и публичный ключ аналитики могут быть допустимы; пароль, приватный токен и секрет подписи — нет.</div>
<p>Не путайте серверные переменные среды с теми, которые сборщик заменяет в браузерном коде. Также не подставляйте неэкранированное значение окружения в HTML или JS как строку: это может привести к синтаксическим ошибкам или внедрению кода.</p>

<h5>11. Source maps и отладка production</h5>
<p><b>Source map</b> связывает минифицированный или преобразованный файл с исходниками, чтобы debugger показывал понятные строки. Карты увеличивают объём артефактов и могут раскрыть исходный код и комментарии. Решите заранее, где их хранить: публично рядом с JS или приватно в системе мониторинга ошибок. Отключить ссылку в браузерных файлах недостаточно, если сами <code class="i">.map</code> всё ещё лежат на общедоступном CDN.</p>
<p>Для диагностики сравните: исходный размер, gzip/brotli размер и фактически переданные байты; откройте Network, проверьте waterfall, повторные чанки и ошибки загрузки динамического импорта. При deploy старый открытый таб может запросить уже удалённый чанк и получить 404 — нужен безопасный порядок выкладки и стратегия обработки устаревшей страницы.</p>

<h5>12. Кеширование артефактов и атомарность релиза</h5>
<p>Имена с content hash меняются при изменении содержимого. Поэтому HTML можно кэшировать недолго или перепроверять, а версионированные JS/CSS — долго с <code class="i">Cache-Control: public, max-age=31536000, immutable</code>. HTML должен ссылаться на согласованный комплект ресурсов одной версии.</p>
<p>Опасный сценарий: новый HTML уже опубликован, а новые чанки ещё не загружены; либо старый HTML остался в кеше, а старые файлы удалены. Публикуйте ассеты до переключения HTML, сохраняйте предыдущие хэшированные файлы некоторое время и по возможности используйте атомарный release-directory/symlink switch. Для SPA настройте fallback на HTML только для навигационных URL, не маскируя 404 ассетов.</p>

<h5>13. TypeScript, транспиляция и проверка типов</h5>
<p><b>Транспиляция</b> преобразует синтаксис (например TS/JSX) в JavaScript. Многие быстрые трансформеры удаляют типы, но не проверяют их. Поэтому production build может пройти при ошибке типов, если отдельный этап <code class="i">tsc --noEmit</code> не включён в CI. Трансформер и type checker решают разные задачи.</p>
<p><b>Target</b> определяет, какой синтаксис/возможности языка нужно сохранить или преобразовать. Синтаксическая трансформация не обязательно добавляет polyfill для отсутствующих API: поддержка <code class="i">Promise</code>, <code class="i">Array.prototype.at</code> и других возможностей требует отдельного решения. Browserslist и настройки target задают аудиторию, но не заменяют проверку совместимости runtime API.</p>

<h5>14. Диагностика больших и медленных сборок</h5>
<ol><li>Зафиксируйте базовую точку: время cold/warm build, размер каждого entry/chunk, сжатый размер, LCP/INP на целевом сценарии.</li><li>Сгенерируйте отчёт состава бандла: например visualizer для Rollup/Vite или bundle analyzer для Webpack. Ищите дубли версий, локали, тяжёлые parser/editor/chart пакеты и случайный импорт серверного кода.</li><li>Проверьте network waterfall и coverage: большой файл может быть быстрым на хорошем интернете, но дорогим при parse/execute на слабом CPU.</li><li>Разделяйте по маршрутам и по действию пользователя; не откладывайте код, нужный первому экрану.</li><li>Проверьте sourcemap, минификацию, compression на CDN и корректные cache headers.</li><li>Повторите измерение в одинаковых условиях. Оптимизация без измерения легко ухудшает загрузку.</li></ol>
<p><b>Initial bundle</b> — код начального сценария; <b>transfer size</b> — переданный по сети размер после сжатия; <b>parsed/executed cost</b> — цена разбора и выполнения JS. Сравнивайте все три: маленький gzip-файл может всё равно долго разбираться на мобильном процессоре.</p>

<h5>15. Короткое сравнение Vite и Webpack</h5>
<table><tr><th>Критерий</th><th>Vite</th><th>Webpack</th></tr>
<tr><td>Типичная модель dev</td><td>ESM dev-server, on-demand трансформации, dependency optimization</td><td>компиляция графа с dev-оптимизациями, кэшем и HMR</td></tr>
<tr><td>Production</td><td>оптимизированная сборка через актуальный backend выбранной версии</td><td>настраиваемая компиляция и оптимизации Webpack</td></tr>
<tr><td>Конфигурация</td><td>меньше стартовой настройки для типичного SPA, расширяется плагинами</td><td>глубокая настройка и зрелая экосистема loader-ов/plugins</td></tr>
<tr><td>Выбор</td><td>новый проект, быстрый цикл разработки, подходящие плагины</td><td>существующая инфраструктура, специальные интеграции, Module Federation или сложные требования</td></tr></table>
<p>Выбор — не рейтинг «новое против старого». Учитывайте текущую кодовую базу, плагины, SSR/библиотечный режим, требования к окружению, стоимость миграции и знания команды. Для ответа на собеседовании назовите сценарий и компромиссы, а не только любимый инструмент.</p>

<h5>16. Практические задачи</h5>
<ol><li><b>Посчитать критический JS.</b> Возьмите build report приложения, выделите файлы, нужные до первого взаимодействия, запишите raw и gzip/brotli размеры. Объясните, почему сумма всех чанков не равна первичной загрузке.</li><li><b>Найти случайный импорт.</b> Найдите тяжёлый модуль в анализаторе, проследите цепочку импорта до точки входа и предложите динамическую загрузку с корректным loading/error состоянием.</li><li><b>Разобрать tree-shaking.</b> Создайте пакет с двумя экспортами и модулем регистрации side effect. Проверьте, что удаляется, а затем измените <code class="i">sideEffects</code> и объясните, почему CSS может пропасть.</li><li><b>Проверить чанки.</b> Добавьте lazy-маршрут и тяжёлую функцию экспорта. Проверьте Network до и после действия, затем отключите кеш и сравните waterfall.</li><li><b>Устроить релиз без 404.</b> Смоделируйте ситуацию, когда старый tab запрашивает старый чанк после деплоя. Предложите порядок публикации файлов и UX fallback.</li><li><b>Сопоставить dev и prod.</b> Запустите production preview за подкаталогом, проверьте base URL, asset paths, env-конфигурацию, sourcemaps и заголовки кеша.</li><li><b>Объяснить трансформер.</b> Удалите ошибку в TS типах, оставив сборку через быстрый трансформер, и покажите, какой CI шаг должен поймать ошибку.</li></ol>

<h5>17. Вопросы на собеседовании: ответ по структуре</h5>
<ul><li><b>Почему Vite быстрее стартует?</b> Объясните ESM dev-модель, on-demand обработку и pre-bundling зависимостей; добавьте, что результат зависит от проекта и конфигурации.</li><li><b>Что даёт tree-shaking?</b> Определение, роль статического графа ESM, side effects и ограничения формата пакета.</li><li><b>Зачем динамический import?</b> Отложить загрузку необязательного кода, уменьшить начальный сценарий; упомянуть состояние загрузки, ошибки и возможные лишние чанки.</li><li><b>Как найти лишний вес?</b> Измерить, построить карту состава, проверить путь импорта/дубли, изменить, снова измерить.</li><li><b>Чем loader отличается от plugin?</b> Loader преобразует модуль/ресурс; plugin участвует в более широком жизненном цикле компиляции и может менять артефакты.</li><li><b>Почему после релиза чанк даёт 404?</b> Несогласованные версии HTML и ассетов, кеш/долгоживущая вкладка или преждевременное удаление файлов; назвать порядок публикации и fallback.</li></ul>

<h5>Словарь терминов этой темы</h5>
<p><b>Артефакт</b> — результат сборки, который разворачивают. <b>Resolver</b> — алгоритм поиска файла по импорту. <b>Трансформер</b> — преобразователь исходного модуля. <b>Минификация</b> — сокращение кода с сохранением поведения. <b>Манглинг</b> — переименование локальных идентификаторов в короткие имена. <b>Runtime</b> — код инструмента, который остаётся в приложении и выполняется в браузере. <b>Invalidation</b> — пометка кеша или артефакта как устаревшего. <b>Waterfall</b> — временная диаграмма сетевых запросов. <b>Deployment</b> — публикация версии приложения в окружении.</p>` }
