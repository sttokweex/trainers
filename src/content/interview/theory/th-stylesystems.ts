import type { TheoryArticle } from '@/engine/types'

export const styleSystems: TheoryArticle = {
  id: 'th-style-systems', topic: 'Tailwind и Sass', title: 'Tailwind CSS и SCSS: от CSS-основ к архитектуре стилей',
  lead: 'Подробное руководство по utility-first CSS, Tailwind v3/v4, Sass/SCSS, каскаду, design tokens, сборке, доступности и выбору между подходами.',
  body: `
<h5>Сначала модель CSS, потом надстройка</h5>
<p>Tailwind и Sass не заменяют браузерный CSS. В итоге браузер получает CSS-правила и применяет обычные механизмы каскада, наследования, специфичности, layout и painting. Tailwind — прежде всего метод именования готовых деклараций классами и генератор CSS по использованным классам. Sass/SCSS — язык-препроцессор, который до браузера компилируется в CSS и даёт переменные, функции, миксины и модульную систему. Их можно применять совместно, хотя часто Tailwind снижает потребность в Sass для компонентных стилей.</p>

<h5>Utility-first: как читать Tailwind</h5>
<p>Utility — небольшой класс, отвечающий за конкретное свойство или согласованную группу деклараций: <code class="i">flex</code>, <code class="i">gap-4</code>, <code class="i">rounded-lg</code>, <code class="i">text-slate-700</code>. Композиция классов задаёт внешний вид прямо у разметки. Это переносит часть решения о стиле к месту использования компонента, сокращает случайные локальные имена и делает визуальные изменения заметными при чтении шаблона.</p>
<pre class="code">&lt;button class="inline-flex items-center gap-2 rounded-lg bg-blue-700 px-4 py-2 font-semibold text-white hover:bg-blue-800 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-700 disabled:cursor-not-allowed disabled:opacity-50"&gt;
  Сохранить
&lt;/button&gt;</pre>
<p>Длинный список классов не автоматически хорош: повторяющиеся сочетания требуют компонентной границы или композиции, но абстрагировать каждую пару утилит тоже не нужно. Извлекайте компонент, когда есть самостоятельное поведение, семантика, повторное использование или согласованный вариант дизайна. Не создавайте компонент только ради короткой строки.</p>

<h5>Responsive, state и доступность</h5>
<p>Вариант перед utility ограничивает правило условием. Префикс breakpoint обычно означает «от этой ширины и выше» (mobile-first): базовые классы задают малый viewport, затем <code class="i">md:</code> или <code class="i">lg:</code> меняют layout. Это не магические устройства: брейкпоинт выбирают по моменту, когда содержимое перестаёт помещаться. Есть варианты состояний вроде <code class="i">hover:</code>, <code class="i">focus-visible:</code>, <code class="i">disabled:</code>, групповые состояния родителя, peer-состояния соседнего элемента, dark mode и reduced motion.</p>
<p>Классы не делают интерфейс доступным сами по себе. Для кнопки нужен правильный HTML element, видимый фокус, контраст, доступное имя, состояние disabled и поведение с клавиатуры. Проверяйте zoom, системный reduced-motion, forced-colors/high-contrast и реальные длинные переводы. Responsive — адаптация к доступному пространству, а не список фиксированных телефонов.</p>

<h5>Design tokens и произвольные значения</h5>
<p>Design token — именованное решение дизайн-системы: цвет действия, радиус карточки, шаг отступов, высота слоя. Tailwind theme переводит токены в классы и CSS variables. Утилиты с произвольными значениями в квадратных скобках помогают для редких случаев, но постоянные повторы лучше превращать в токен: иначе проект получает множество почти одинаковых цветов и размеров без единого места изменения.</p>
<p>CSS custom properties живут в браузере и могут меняться через каскад, темы, media queries и JS. Переменная конфигурации Sass вычисляется при сборке и после компиляции исчезает. Это разные инструменты: Sass хорош для вычислений во время разработки; CSS variable нужна, если значение должно изменяться в runtime или наследоваться компонентами.</p>

<h5>Tailwind v3 и v4: что принципиально различается</h5>
<p>В Tailwind v3 типичная конфигурация задаёт массив <code class="i">content</code> с путями сканирования шаблонов, а тема расширяется в JavaScript-конфиге. В Tailwind v4 основной путь — CSS-first: импорт Tailwind и theme variables задаются в CSS, а источник классов обнаруживается автоматически с возможностью явного управления. v4 меняет внутреннюю архитектуру и интеграционные defaults; рецепт из v3 нельзя механически считать актуальным для v4. При разборе проекта сначала посмотрите фактическую major-версию, plugin интеграции и файл конфигурации.</p>
<p>В обеих версиях production CSS ограничивается найденными классами/источниками, но детали компилятора и настройки различаются. Генератор не исполняет произвольную логику приложения ради вычисления класса: source extraction статичен по своей природе. Поэтому это не runtime CSS-in-JS.</p>

<h5>Динамические классы, extraction и production</h5>
<p>Такой код ненадёжен для сканера: <code class="i">bg- + переменная color + -600</code>. Цельные имена классов не существуют в исходном тексте, и production CSS может их не включить. Выбирайте класс из явной таблицы разрешённых значений или создавайте style/custom property для действительно произвольного пользовательского цвета. Safelist — допустимое точечное исключение, но широкая safelist раздувает CSS и скрывает проблему дизайна.</p>
<pre class="code">const toneClass = {
  blue: 'bg-blue-700 hover:bg-blue-800',
  red: 'bg-red-700 hover:bg-red-800',
} as const
const className = toneClass[tone]
</pre>
<p>Проверьте, что сканируются все пакеты workspace и файлы шаблонов. Классы из внешней component library могут отсутствовать в исходниках приложения. Сравнивайте CSS build size, критический CSS и фактическую загрузку, а не количество utility-токенов в JSX.</p>

<h5>SCSS — синтаксис Sass с фигурными скобками</h5>
<p>Sass имеет два синтаксиса: indented Sass использует отступы, SCSS — CSS-подобные фигурные скобки и точки с запятой. SCSS компилируется инструментом Sass до CSS. Он не исполняется браузером. Современное расширение Sass — переменные <code class="i">$name</code>, вложенность, миксины и функции, но CSS постепенно получил собственные custom properties, nesting, <code class="i">calc()</code>, <code class="i">color-mix()</code> и другие возможности; не следует включать Sass только по привычке.</p>
<pre class="code">$space-4: 1rem;
.card {
  padding: $space-4;
  &amp;:hover { box-shadow: 0 0.5rem 1rem #0002; }
  .card__title { margin: 0; }
}
</pre>
<p>Вложенность облегчает локальное чтение, но глубокие селекторы увеличивают связанность и специфичность. Держите nesting неглубоким; не отражайте всю DOM-иерархию в CSS. Символ <code class="i">&amp;</code> подставляет родительский селектор, поэтому <code class="i">&amp;--large</code> создаёт BEM-модификатор, а <code class="i">&amp;:hover</code> — псевдокласс.</p>

<h5>Миксины, функции, циклы и цена абстракции</h5>
<p><b>Mixin</b> вставляет набор CSS-деклараций, возможно с аргументами и content block. Он уместен для повторяемого паттерна, но каждое включение может продублировать CSS. <b>Function</b> вычисляет и возвращает значение при компиляции. <code class="i">@each</code>/<code class="i">@for</code> могут породить много селекторов и раздувать итог. Не моделируйте весь дизайн API Sass-функциями, если простая CSS переменная, класс или компонент прозрачнее.</p>
<pre class="code">@mixin focus-ring($color) {
  &amp;:focus-visible {
    outline: 2px solid $color;
    outline-offset: 2px;
  }
}
.button { @include focus-ring(#1d4ed8); }
</pre>

<h5>@use и @forward вместо глобального @import</h5>
<p>Старый Sass <code class="i">@import</code> подмешивал объявления в глобальное пространство имён, мог многократно выдавать CSS и усложнял понимание источника переменной. Sass-модульная система использует <code class="i">@use</code> для обращения к членам через namespace и <code class="i">@forward</code> для создания публичного фасада библиотеки. Это снижает коллизии и делает зависимости явными. CSS <code class="i">@import</code> — отдельная браузерная директива, это не тот же механизм, что удаляемый Sass import.</p>
<pre class="code">// _tokens.scss
$brand: #2457d6;

// button.scss
@use 'tokens';
.button { background: tokens.$brand; }
</pre>

<h5>Выбор подхода и рабочая дисциплина</h5>
<p>Tailwind подходит, когда команда согласна на utility-first стиль и хочет быстро собирать интерфейсы из токенов без большого набора локальных селекторов. SCSS подходит существующей CSS-архитектуре, сложным тематическим таблицам, стилям legacy-продукта и библиотекам с ясными абстракциями. Их можно сочетать, например Tailwind для layout и SCSS для сложного canvas/editor компонента. Решение зависит от design system, команды, способа доставки компонентной библиотеки и поддержки тем.</p>
<p>Проверяйте итоговый CSS, каскад слоёв и порядок подключения, CSS Modules/global reset, специфичность, purge/source scanning, critical rendering и source maps. В дизайн-системе фиксируйте токены, состояния, responsive правила и соглашения о компонентах. Избегайте двух источников истины для одних токенов без генерации из общего набора.</p>

<h5>Практика: перестройте панель фильтров</h5>
<ol><li>Сверстайте фильтры на семантических label, input и button, включая клавиатуру и видимый focus.</li><li>Сделайте мобильный одноколоночный layout и breakpoint, выбранный по ширине контента.</li><li>Реализуйте тему CSS custom properties и объясните, почему они нужны вместо Sass variables.</li><li>Перенесите стили в Tailwind и зафиксируйте варианты через явную map, не конкатенацию фрагментов class.</li><li>Сделайте SCSS вариант с @use, token module и focus-ring mixin; сравните читаемость итогового CSS.</li><li>Соберите production и найдите класс, который теряется из-за динамической сборки имени.</li></ol>
<p><b>Собеседовательная проверка:</b> объясните, почему класс в dev-сборке может отсутствовать в production; чем переменная Sass отличается от CSS custom property; почему чрезмерная вложенность ухудшает поддержку; как Tailwind v3 отличается от v4 в конфигурации.</p>
`,
}
