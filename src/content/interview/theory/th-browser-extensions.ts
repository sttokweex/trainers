import type { TheoryArticle } from '@/engine/types'

export const browserExtensions: TheoryArticle = { id:'th-browser-extensions', topic:'Браузер', title:'Браузерные расширения: Manifest V3, контексты и безопасность',
  lead:'Как устроены расширения Chromium: service worker, content scripts, permissions, обмен сообщениями, хранение, публикация и типичные уязвимости.',
  body:`
<h5>Из чего состоит расширение</h5>
<p>Расширение — пакет кода и деклараций, который браузер устанавливает с определёнными разрешениями. Оно может добавлять UI (popup, side panel, options page), реагировать на события браузера в background-контексте, внедрять content script в страницы и обращаться к API расширений. Конкретный набор API зависит от браузера и версии manifest.</p>
<p><b>Manifest</b> — файл метаданных: версия схемы, имя, entry points, permissions, host permissions, content scripts, CSP и ресурсы, доступные страницам. Manifest не просто конфиг сборщика: его разрешения влияют на границу доверия и запрашиваемый доступ.</p>

<h5>Manifest V3 и background service worker</h5>
<p>В Chrome Manifest V3 фоновые события обрабатывает extension service worker. Он может быть остановлен браузером, когда нет работы, и запущен снова при событии. Поэтому в памяти нельзя хранить единственное состояние, которое необходимо пережить остановку. Обработчики событий нужно регистрировать синхронно на верхнем уровне, а состояние сохранять в подходящем storage или восстанавливать из источника истины.</p>
<p>Service worker не является постоянно работающим Node-процессом: нет DOM страницы, а браузер управляет его временем жизни. Не полагайтесь на setTimeout как на гарантированное расписание или вечное websocket-соединение. Для периодических задач существуют браузерные механизмы наподобие alarms, но точность, квоты и ограничения нужно сверять с документацией браузера.</p>
<p>В Manifest V3 исполняемый код расширения должен быть поставлен в пакете и проверяем; удалённая загрузка произвольного исполняемого JS ограничена политиками магазина. Конфигурацию/данные можно загружать удалённо, но превращать их в код через eval/new Function — плохой и часто запрещённый дизайн.</p>

<h5>Контексты: popup, service worker и content script</h5>
<table><tr><th>Контекст</th><th>Есть DOM страницы?</th><th>Типичные задачи</th><th>Жизненный цикл</th></tr>
<tr><td>Popup/options/side panel</td><td>DOM собственного UI</td><td>настройки и пользовательские действия</td><td>popup часто закрывается при потере фокуса</td></tr>
<tr><td>Extension service worker</td><td>нет DOM</td><td>события браузера, координация, privileged API</td><td>может останавливаться и запускаться снова</td></tr>
<tr><td>Content script</td><td>доступ к DOM страницы</td><td>чтение/изменение UI сайта, DOM-события</td><td>обычно связан с вкладкой/документом</td></tr>
<tr><td>Страница сайта</td><td>да, собственная страница</td><td>обычный web-код</td><td>контролируется сайтом, не extension runtime</td></tr></table>

<h5>Изолированный мир content script</h5>
<p>Content script может видеть и менять DOM страницы, но его JavaScript-контекст обычно изолирован от JS-переменных сайта (isolated world). Сайт не должен напрямую читать внутренние переменные content script, а content script не получает автоматически JS-объекты приложения. Однако DOM общий: страница видит изменения DOM, может менять его параллельно, а небезопасный DOM bridge способен открыть путь кода страницы в привилегированный контекст расширения.</p>
<p>Если действительно нужно взаимодействовать с JS-событиями страницы, отдельный скрипт в MAIN world работает в её контексте и не имеет тех же привилегий extension API. Считайте все сообщения с DOM/MAIN world недоверенными данными: валидируйте тип, размер и допустимое действие в изолированном/privileged контексте.</p>

<h5>Permissions и host permissions</h5>
<p><code class="i">permissions</code> запрашивает доступ к API браузера; <code class="i">host_permissions</code> — к сайтам/URL. Широкий шаблон вроде <code class="i">&lt;all_urls&gt;</code> увеличивает последствия ошибки и снижает доверие пользователя. Предпочитайте least privilege: минимальный набор сайтов, optional permissions по пользовательскому действию и отказ от прав, которые не используются.</p>
<p>Content scripts также ограничиваются matches и настройками injection. Совпадение host permission не означает, что следует исполнять обработку на любой странице. Не вставляйте secrets в код пакета: extension bundle доступен установленному пользователю и может быть исследован.</p>

<h5>Обмен сообщениями между контекстами</h5>
<p>Контексты общаются через runtime messaging: popup/content script отправляет сообщение background worker-у, тот проверяет отправителя и выполняет ограниченное действие. В API бывают одноразовые сообщения и долгоживущие ports; детали return Promise/asynchronous response различаются между версиями браузера и API. Проектируйте контракт как публичный протокол: <code class="i">type</code>, версия, schema validation, timeout, correlation id и контролируемые ошибки.</p>
<pre class="code">// идея контракта, не принимайте произвольные команды
{ type: 'tab:extract-title', version: 1, requestId: '...' }

// ответ
{ type: 'tab:extract-title:result', requestId: '...', title: '...' }</pre>
<p>В обработчике background проверяйте <code class="i">sender.id</code>, <code class="i">sender.tab</code>, URL/frame при необходимости, схему данных и разрешённость действия. Не создавайте универсальный «выполни этот JS» API. Учитывайте, что сообщение может прийти от контекста, который закрывается, и ответ — не подтверждение долговечной записи.</p>

<h5>Хранение и синхронизация</h5>
<p>API storage расширений асинхронен и может давать local/sync/managed области с разными лимитами и политиками. <code class="i">storage.sync</code> — ограниченная синхронизация настроек между устройствами пользователя, не база данных для больших объёмов. Для локальных данных используйте подходящую local storage область или IndexedDB, контролируйте миграцию схемы и quota errors.</p>
<p>Popup может закрыться посреди операции; service worker может остановиться. Долговечные операции должны иметь состояние/идемпотентность, а UI после запуска читать актуальное состояние. Не храните пароль сайта, refresh token пользователя или историю просмотра без ясной причины, защиты, минимизации и прозрачного согласия.</p>

<h5>Сеть, CORS и доступ к сайтам</h5>
<p>Fetch из page/content context и fetch из privileged extension context могут подчиняться разным правилам доступа; CORS не является аутентификацией сервера. Host permission может разрешить расширению обращаться к хосту через API браузера, но нужно отдельно проверить правила конкретного браузера, запрос пользователя и нужные заголовки. Никогда не отправляйте пользовательские страницы/куки на сторонний сервер без явной необходимости и прозрачного согласия.</p>
<p>Расширение не должно обходить защиту сервера. Если оно делает запрос от лица пользователя, учитывайте CSRF, cookie credentials, CORS, отзыв сессии и обработку данных; сервер всё равно проверяет полномочия.</p>

<h5>Безопасность расширений</h5>
<ul><li>Минимизируйте permissions и просите optional доступ в момент понятного действия.</li><li>Не используйте eval и небезопасную вставку HTML; content script обрабатывает враждебный DOM.</li><li>Не доверяйте сообщениям между контекстами и странице, даже если сообщение пришло из ожидаемой вкладки.</li><li>Не храните секреты в bundle; защищайте чувствительные данные хранением ОС/браузера и не логируйте их.</li><li>Ограничьте web-accessible resources: опубликованный ресурс может быть доступен страницам и становится частью поверхности атаки.</li><li>Проверяйте CSP расширения, зависимости и обновления; supply-chain компрометация dependency может получить привилегии расширения.</li><li>Не собирайте лишние данные browsing history; минимизируйте, ограничивайте срок, объясняйте пользователю цель.</li></ul>

<h5>Тестирование и публикация</h5>
<p>Тестируйте unit-логику отдельно от browser APIs, затем интеграционные сценарии в настоящем браузере: установка/обновление, popup закрывается, service worker перезапускается, вкладка навигирует на другой URL, iframe/frame, запрет permission, квота storage, offline и браузерные различия. Chrome и Firefox API не идентичны; используйте compatibility layer только для реально поддерживаемых общих сценариев.</p>
<p>Публикация в Web Store/Add-ons — отдельный pipeline: собирайте воспроизводимый пакет, проверяйте manifest и permissions, сканируйте зависимости, не включайте source secrets, ведите версионирование/миграции storage и процедуру отзыва/rollback. Ошибку обновления нельзя исправить тем, что popup работает на машине разработчика.</p>

<h5>Типичный вопрос на собеседовании</h5>
<p>«Почему после нескольких минут расширение перестало отвечать?» Проверьте, не был ли остановлен background service worker и не держалось ли состояние только в памяти. Затем посмотрите журналы расширения, регистрацию listeners, Promise/response lifecycle, permissions и отправителя сообщения. Архитектурно восстановите состояние из storage, сделайте обработку идемпотентной и добавьте telemetry без чувствительных данных.</p>` }
