import type { TheoryArticle } from '@/engine/types'

export const loadBalancing: TheoryArticle = {
  id: 'th-load-balancing',
  topic: 'Надёжность и эксплуатация',
  title: 'Балансировщики: как запрос доходит до нужного сервера',
  lead: 'Разбираем reverse proxy, L4 и L7, алгоритмы выбора узла, проверки здоровья, сессии, TLS и безопасное выключение сервера.',
  body: `
<h5>Зачем нужен балансировщик</h5>
<p>Если приложение работает на одном сервере, его адрес можно указать прямо в клиенте. Но один сервер ограничивает пропускную способность и становится единственной точкой отказа. <b>Балансировщик нагрузки</b> принимает запросы клиентов и распределяет их между несколькими экземплярами приложения. Клиент знает адрес балансировщика, а внутренние адреса серверов обычно скрыты.</p>
<p><b>Экземпляр</b> (instance) — один запущенный процесс или контейнер приложения. <b>Upstream</b> — группа серверов, куда прокси может передать запрос. <b>Reverse proxy</b> (обратный прокси) принимает запрос со стороны клиента и обращается к серверу от его имени; обычный forward proxy, напротив, действует от имени клиента при выходе в сеть.</p>
<pre class="code">Браузер
  → DNS находит публичный адрес
  → CDN может отдать закешированный файл
  → балансировщик принимает соединение
  → один здоровый экземпляр API
  → база данных и другие зависимости</pre>
<div class="key">Балансировщик не делает серверы автоматически масштабируемыми. Он распределяет уже существующие экземпляры. Чтобы выдержать отказ экземпляра, приложение должно хранить важное состояние вне локальной памяти сервера или уметь восстановить его.</div>

<h5>Два уровня: L4 и L7</h5>
<p><b>L4</b> — четвёртый уровень модели сети, транспортный уровень: TCP или UDP. Балансировщик L4 видит адреса и порты соединения, но обычно не читает путь URL и заголовки HTTP. Он направляет соединение целиком. Это дешевле и подходит для произвольного TCP/UDP-трафика.</p>
<p><b>L7</b> — седьмой, прикладной уровень. HTTP-балансировщик может прочитать метод, домен, путь и заголовки: например, отправить <code class="i">/api</code> в API, а <code class="i">/images</code> — в сервис изображений. Он также может завершать TLS, ограничивать частоту запросов, сжимать ответы и добавлять заголовки.</p>
<table>
<tr><th>Вопрос</th><th>L4</th><th>L7</th></tr>
<tr><td>Что выбирает маршрут</td><td>IP, порт, протокол</td><td>HTTP-домен, путь, метод, заголовки</td></tr>
<tr><td>Читает ли тело HTTP</td><td>нет</td><td>может, если правило этого требует</td></tr>
<tr><td>Накладные расходы</td><td>обычно ниже</td><td>выше: запрос нужно разобрать</td></tr>
<tr><td>Примеры</td><td>TCP-прокси, балансировка базового TCP-трафика</td><td>маршрутизация API и веб-приложения</td></tr>
</table>
<p>Названия L4/L7 описывают способ принятия решения, а не обязательно отдельные коробки. Один продукт может совмещать оба режима. <b>DNS-балансировка</b> — другой механизм: DNS выдаёт разные IP-адреса, но не видит отдельный HTTP-запрос и не проверяет, что конкретный процесс готов его принять. Из-за DNS-кеша переключение адреса может занять время.</p>

<h5>Как выбрать экземпляр</h5>
<ul>
<li><b>Round robin</b> — по очереди: A, B, C, снова A. Прост и работает, если серверы примерно одинаковы и запросы похожи по цене.</li>
<li><b>Weighted round robin</b> — чаще отправлять на более мощные серверы. Вес задаёт долю запросов, но не гарантирует одинаковую текущую загрузку.</li>
<li><b>Least connections</b> — выбирать сервер с наименьшим числом активных соединений. Полезно, когда запросы длятся разное время; число соединений всё равно лишь приближение к реальной загрузке CPU.</li>
<li><b>Least response time</b> — учитывать наблюдаемое время ответа и активные запросы. Нужно понимать, как система сглаживает короткие всплески и не начинает отправлять всё на один временно быстрый узел.</li>
<li><b>Hash по ключу</b> — один и тот же пользователь или ключ обычно попадает на один сервер. Это называют affinity (привязка). При изменении состава серверов обычный hash может перераспределить много ключей; consistent hashing (согласованное хеширование) уменьшает эту долю.</li>
</ul>
<p>Для обычного stateless API часто достаточно round robin или least connections. <b>Stateless</b> означает, что конкретный сервер не обязан помнить прошлый запрос пользователя в своей оперативной памяти: любой экземпляр может обработать следующий запрос, опираясь на БД, общий кеш или токен. Это не значит, что приложение вообще не хранит данные.</p>

<h5>Проверки здоровья: живой процесс ещё не готов обслуживать запросы</h5>
<p><b>Health check</b> (проверка здоровья) помогает перестать направлять трафик на сломанный узел. Есть два распространённых вида:</p>
<ul>
<li><b>Liveness</b> отвечает «процесс ещё работает?». Если нет, оркестратор может перезапустить его.</li>
<li><b>Readiness</b> отвечает «этот экземпляр готов принимать новые запросы?». Если нет, балансировщик временно исключает его из маршрутизации.</li>
</ul>
<p>Не делайте liveness-проверку зависимой от каждой внешней системы. Если на короткое время недоступна база, перезапускать все API-процессы обычно только хуже. Readiness может проверять необходимые зависимости, но быстрый сбой всей базы не должен создавать бесконечную волну перезапусков. У проверок есть таймаут, интервал, число неудач до исключения и число успехов до возврата.</p>
<p><b>Health check не доказывает корректность всех запросов.</b> Он только даёт ограниченный сигнал о готовности. Для обнаружения перегрузки важны также таймауты, доля ошибок, очередь запросов и пользовательская задержка.</p>

<h5>Сессии и «липкие» запросы</h5>
<p>Если состояние сессии лежит в памяти экземпляра A, а следующий запрос пользователя попал в B, сервер B его не найдёт. Возможны два решения:</p>
<ol>
<li><b>Общее хранилище</b>: сессия хранится в Redis или БД, доступной всем экземплярам. Это обычно упрощает масштабирование и переключение при отказе.</li>
<li><b>Sticky sessions</b> (липкие сессии): балансировщик старается отправлять пользователя на тот же экземпляр, часто по cookie. Это может быть временным обходом, но не спасает при отказе узла; нагрузка распределяется хуже, а перенос трафика сложнее.</li>
</ol>
<p>JWT-токен тоже не делает всю систему stateless сам по себе. Он позволяет проверять часть удостоверения пользователя без серверной сессии, но отзыв токена, смена прав и хранение другого пользовательского состояния всё равно требуют продуманного решения. Пользователя и права сервер должен проверять сам, а не доверять одному факту наличия токена.</p>

<h5>TLS, IP клиента и заголовки прокси</h5>
<p><b>TLS</b> — криптографическая защита соединения HTTPS: подтверждает сервер сертификатом и шифрует данные. Балансировщик может <b>завершить TLS</b>: расшифровать внешний трафик и отправить HTTP внутри доверенной сети. Либо он может передавать зашифрованное соединение дальше, не читая HTTP. Первый вариант упрощает сертификаты и правила маршрутизации; второй сохраняет сквозное шифрование до приложения.</p>
<p>После завершения TLS приложение может получить сведения о внешнем запросе через <code class="i">Forwarded</code> или распространённые <code class="i">X-Forwarded-For</code>, <code class="i">X-Forwarded-Proto</code>, <code class="i">X-Forwarded-Host</code>. Эти заголовки достоверны только если их записал доверенный прокси. Если приложение принимает поддельный <code class="i">X-Forwarded-For</code> напрямую от клиента, злоумышленник может подменить IP для журнала, ограничения частоты или проверки доступа.</p>
<div class="warn">Настройка «доверять всем прокси» опасна. Укажите точные адреса или число доверенных прокси-хопов; публичный клиент не должен иметь возможность самостоятельно задавать доверенный адрес.</div>

<h5>Таймауты, повторы и перегрузка</h5>
<p>У балансировщика и приложения разные таймауты: ожидание установления соединения, ожидание ответа от сервера, простой keep-alive. Их задают осознанно. Таймаут балансировщика должен быть согласован с таймаутом приложения и внешних зависимостей, иначе клиент увидит 504, хотя обработка продолжится и займёт ресурсы.</p>
<p><b>502 Bad Gateway</b> обычно означает, что прокси не получил корректный ответ upstream; <b>503 Service Unavailable</b> — что сейчас нет доступного или готового сервиса; <b>504 Gateway Timeout</b> — что upstream не ответил вовремя. Это типовые значения, а конкретную причину нужно смотреть по журналам и метрикам прокси и приложения.</p>
<p>Не повторяйте запрос автоматически при любой ошибке. Повтор безопасен, если операция идемпотентна: повтор не создаёт второй заказ и не списывает деньги второй раз. Если балансировщик и клиент оба делают по три повтора, одна исходная операция может превратиться в девять запросов. Задавайте общий бюджет времени, ограничивайте число попыток и добавляйте случайную паузу (jitter), чтобы клиенты не повторяли запрос синхронно.</p>
<p>При перегрузке очередь запросов может расти быстрее, чем сервер успевает её разгребать. Это увеличивает задержку, удерживает соединения и усугубляет сбой. Полезнее быстро отклонить часть запросов с понятным 503 или применить rate limit, чем держать бесконечную очередь.</p>

<h5>Безостановочное выключение и выкладка</h5>
<p><b>Connection draining</b> — мягкое выведение экземпляра из обслуживания. Балансировщик перестаёт посылать ему новые запросы, но даёт завершиться уже начатым. Затем приложение закрывает свои соединения и завершает процесс. Без этого при деплое можно оборвать загрузку файла, WebSocket или долгий запрос.</p>
<ol>
<li>Новый экземпляр запускается и проходит readiness-проверку.</li>
<li>Балансировщик начинает отправлять ему трафик.</li>
<li>Старый экземпляр исключают из новых назначений и дают текущим запросам закончиться.</li>
<li>Старый процесс завершает фоновые задачи или возвращает их в очередь и останавливается.</li>
</ol>
<p>Для WebSocket draining требует отдельной политики: соединение может жить часами. Можно послать клиенту сообщение о переподключении, закрыть соединение с кодом, который разрешает повтор, и обеспечить восстановление через другой экземпляр.</p>

<h5>Короткий ответ на собеседовании</h5>
<div class="key"><b>Пример:</b> «Поставлю L7 reverse proxy перед несколькими stateless API-экземплярами. Балансировщик исключает узлы по readiness, завершает TLS и передаёт приложению проверенные proxy-заголовки. Сессии храню совместно, а не в памяти узла. Для выкладки использую draining. Таймауты согласую с API и БД, а повторы разрешаю только для идемпотентных запросов. Метрики смотрю по экземплярам и по пользователю: доля ошибок, p95/p99 и активные соединения».</div>

<h5>Термины главы</h5>
<ul>
<li><b>Пропускная способность</b> — сколько запросов или данных система успевает обработать за единицу времени.</li>
<li><b>Задержка (latency)</b> — сколько времени один запрос проходит от отправки до ответа.</li>
<li><b>Хоп (hop)</b> — один промежуточный узел на пути запроса.</li>
<li><b>Keep-alive</b> — повторное использование уже установленного соединения для следующих запросов.</li>
<li><b>p95/p99</b> — время, быстрее которого завершились 95% или 99% запросов; показывает медленный хвост лучше среднего.</li>
<li><b>Деградация</b> — частичная потеря качества или функций без полного отказа сервиса.</li>
</ul>`
}
