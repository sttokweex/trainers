import type { TheoryArticle } from '@/engine/types'

export const telegramMiniApps: TheoryArticle = { id:'th-telegram-mini-apps', topic:'Браузер', title:'Telegram Mini Apps: WebView, initData и интеграция с ботом',
  lead:'Как веб-приложение запускается внутри Telegram, какие поля интерфейса полезны, как доверять пользователю только после серверной проверки и где границы WebView.',
  body:`
<h5>Что такое Mini App</h5>
<p>Telegram Mini App — веб-приложение, открытое клиентом Telegram в WebView по ссылке/кнопке бота или другому поддерживаемому entry point. Это обычный web-код с дополнительным JavaScript bridge, через который страница может узнать параметры запуска и попросить Telegram-клиент выполнить интерфейсные действия. API и доступные возможности зависят от версии Telegram-клиента и способа запуска.</p>
<p>Не считайте Mini App нативным доверенным приложением: web-страница остаётся под контролем вашего origin, а параметры клиента могут быть модифицированы пользователем. Бизнес-правила, права и платежные ограничения проверяет backend.</p>

<h5>Запуск и клиентский SDK</h5>
<p>Официальный bridge обычно доступен как <code class="i">window.Telegram.WebApp</code> после подключения предоставляемого Telegram скрипта. Полезные возможности: получить init data, сообщить клиенту <code class="i">ready()</code>, развернуть WebView <code class="i">expand()</code>, настроить цвета/заголовок, использовать MainButton/BackButton, открыть ссылку/инвойс и закрыть Mini App. Вызов <code class="i">ready()</code> сообщает клиенту, что интерфейс подготовлен; не блокируйте его бесконечно на необязательном запросе.</p>
<p>Инициализируйте SDK с feature detection, учитывайте несовместимую/старую версию клиента, fallback в обычном браузере и отсутствие bridge в локальной разработке. Темы, safe area, viewport height, клавиатура мобильного устройства, системная кнопка назад и закрытие окна требуют проверки на реальных клиентах iOS/Android/Desktop.</p>

<h5>initData и initDataUnsafe</h5>
<p><code class="i">initData</code> — подписанная строка параметров запуска, которую клиент отправляет вашему backend для валидации. <code class="i">initDataUnsafe</code> — удобная уже разобранная структура для отображения UI, но она пришла из клиентского контекста и до серверной проверки не должна использоваться для аутентификации, списания денег, выдачи доступа или присвоения наград.</p>
<p>Backend проверяет HMAC по алгоритму Bot API, использует token в строго серверном контексте, сравнивает подпись constant-time, валидирует <code class="i">auth_date</code> на свежесть и разбирает поля только после проверки. Не логируйте токен и полную initData строку: она может быть bearer-подобным доказательством запуска пользователя. Подробный алгоритм проверки есть в статье про безопасность и вопросе <code class="i">sec-tg</code>.</p>
<p>Проверка initData подтверждает, что набор параметров сформирован Telegram для данного bot token и времени, но не означает, что пользователь имеет право на любую операцию. После валидации сопоставьте Telegram user id со своей записью и проверяйте бизнес-роль на каждом API запросе. Добавьте защиту от повторного использования старой initData через допустимый возраст и обычную серверную сессию.</p>

<h5>Сессия Mini App и API</h5>
<p>Паттерн: клиент один раз отправляет initData по HTTPS на backend, backend проверяет и обменивает её на вашу сессию с ограниченным сроком/аудиторией. Дальше клиент использует безопасный credential механизм вашей web-архитектуры; не передавайте bot token клиенту и не прикладывайте сырую initData к каждому стороннему сервису. Учтите XSS: скрипт внутри страницы может читать всё, что хранится в JS-памяти.</p>
<p>Если используете cookie, настройте Secure/HttpOnly/SameSite и CSRF-модель под способ запуска WebView. Если используете bearer access token, храните его с пониманием XSS-риска и делайте срок коротким/ротацию. Проверяйте Origin и CORS как браузерные ограничения, но не считайте их авторизацией.</p>

<h5>Кнопки, события и навигация</h5>
<p>MainButton полезна для основного действия, BackButton — для интеграции с историей навигации. Держите состояние UI синхронным: показывайте кнопку только когда действие доступно, блокируйте повторный submit локально, но сервер всё равно делайте идемпотентным. Обрабатывайте Telegram-события темы/viewport/закрытия, снимайте listeners при размонтировании, учитывайте повторную инициализацию React StrictMode.</p>
<p>Открытие внешней ссылки и закрытие WebView должны соответствовать UX и правилам клиента. Не храните единственное состояние корзины/платежа только в памяти мини-приложения: WebView может быть закрыт или восстановлен. Сохраняйте бизнес-процесс на backend и при повторном запуске восстанавливайте актуальное состояние.</p>

<h5>Оплата, награды и доверенная логика</h5>
<p>Цена, скидка, роль, завершение задания, игровые очки и доступ — серверные решения. Клиент присылает intent (например, купить товар id), сервер сам загружает цену из каталога, проверяет пользователя и создаёт заказ. Не принимайте сумму и статус оплаты, вычисленные фронтендом. Проверяйте платёжные update/callback по серверному контракту Telegram, сохраняйте уникальный идентификатор транзакции и делайте обработчик идемпотентным.</p>
<p>Если используете Telegram invoice/payment flows, разделяйте pre-checkout и подтверждение успешной оплаты; отвечайте на запрос подтверждения в требуемый срок и только после проверки условий. После фактического успешного платёжного события переводите заказ в оплаченный статус. Клиентский callback или возврат из invoice UI не является доказательством оплаты.</p>

<h5>Безопасность и приватность</h5>
<ul><li>Только HTTPS, корректные redirect URI и строгая проверка origin на backend.</li><li>Минимизируйте данные Telegram-профиля, которые сохраняете; имя/username могут измениться и не заменяют стабильный user id.</li><li>Контролируйте CSP, сторонние scripts, npm supply chain и XSS: скомпрометированный скрипт увидит initData до отправки.</li><li>Rate limit чувствительные операции, связывайте их с серверной сессией и idempotency key.</li><li>Не используйте данные из initDataUnsafe для прав; client-side проверки нужны лишь для UX.</li><li>Не помещайте bot token, секрет подписи и private credentials в Vite env с публичным префиксом или bundle.</li></ul>

<h5>Тестирование и диагностика</h5>
<p>Нужны unit-тесты проверки подписи: валидная строка, изменённое поле, отсутствующий hash, истёкший auth_date, неверная кодировка, повтор и malformed user. Отдельно тестируйте API права без Telegram UI, а e2e — открытие из бота, theme/viewport, BackButton, закрытие, повторный запуск и сеть offline. Telegram Web/Desktop/Android/iOS отличаются WebView и доступностью методов; логируйте версию SDK и тип платформы без полной initData.</p>
<p>Сделайте mock режима разработки, но отделите его от production сборки и backend: тестовый user id нельзя принимать как настоящую аутентификацию. Проверяйте домен и настройки запуска у BotFather, deep links, HTTPS сертификат, CORS и возврат пользователя после оплаты.</p>

<h5>Как отвечать на собеседовании</h5>
<p>Назовите Mini App web-приложением в Telegram WebView с клиентским bridge. Объясните, что initDataUnsafe годится для отображения, а backend проверяет подписанную initData и возраст. Затем обсудите обмен на серверную сессию, хранение токена, идемпотентность платежей, lifecycle WebView, адаптацию viewport и тесты на разных клиентах. Подчеркните: клиентская кнопка подтверждает намерение, но не право и не факт оплаты.</p>` }
