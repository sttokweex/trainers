import type { TheoryArticle } from '@/engine/types'

export const html: TheoryArticle = {
  id: 'th-html',
  topic: 'CSS и вёрстка',
  title: 'HTML глубже тегов: семантика, формы и загрузка ресурсов',
  lead: 'Как браузер превращает разметку в DOM, зачем нужны семантические элементы и где нативное поведение HTML избавляет от ошибок.',
  body: `
<h5>HTML, DOM и дерево доступности — это не одно и то же</h5>
<p><b>HTML</b> — язык разметки документа. Браузер разбирает полученные байты и строит <b>DOM</b> (Document Object Model) — дерево объектов, с которым работают JavaScript и CSS. Дерево доступности — отдельное представление интерфейса для вспомогательных технологий: скринридер получает имя, роль и состояние элементов, а не просто список HTML-тегов.</p>
<pre class="code">&lt;button aria-pressed="true"&gt;Показывать детали&lt;/button&gt;

DOM: элемент button с атрибутом aria-pressed
Дерево доступности: роль «кнопка», имя «Показывать детали»,
состояние «нажата»</pre>
<p>Браузер исправляет часть неверно вложенной разметки при парсинге. Поэтому итоговый DOM не всегда в точности повторяет исходный текст HTML; для отладки смотрите вкладку Elements, а не только View Source.</p>

<h5>Семантика: выбирайте элемент по действию</h5>
<ul>
<li><code class="i">&lt;a href="…"&gt;</code> — переход к другому адресу или месту страницы. Ссылка открывается в новой вкладке, может копироваться, сохраняться и работать без JavaScript.</li>
<li><code class="i">&lt;button type="button"&gt;</code> — действие на текущей странице: открыть меню, сохранить форму, показать диалог.</li>
<li><code class="i">&lt;nav&gt;</code>, <code class="i">&lt;main&gt;</code>, <code class="i">&lt;header&gt;</code>, <code class="i">&lt;footer&gt;</code> — обозначают области по их смысловой роли. На странице обычно один основной <code class="i">main</code>.</li>
<li><code class="i">&lt;h1&gt;…&lt;h6&gt;</code> — заголовки разделов. Уровень выбирают по структуре документа, а не по нужному размеру шрифта.</li>
<li><code class="i">&lt;ul&gt;</code>, <code class="i">&lt;ol&gt;</code>, <code class="i">&lt;li&gt;</code> — список, где порядок или принадлежность элементов имеют значение.</li>
</ul>
<p>Если вместо ссылки или кнопки собрать интерактивный <code class="i">div</code>, придётся самостоятельно добавлять фокус, клавиатурное управление, роль, состояния, поведение Enter/Space и работу в форме. Нативный элемент уже умеет большую часть этого.</p>
<div class="warn">Не ставьте <code class="i">role="button"</code> на обычный элемент, если можно написать <code class="i">&lt;button&gt;</code>. ARIA добавляет семантическое имя или состояние, но сам по себе не добавляет поведение кнопки.</div>

<h5>Форма — протокол взаимодействия, а не набор полей</h5>
<p><code class="i">&lt;form&gt;</code> группирует поля и задаёт отправку. При отправке браузер запускает нативную проверку, создаёт событие <code class="i">submit</code> и отправляет поля по сети, если обработчик не отменил действие. Нативный сценарий работает и без JS, а кнопка отправки с типом <code class="i">submit</code> может активироваться клавишей Enter.</p>
<pre class="code">&lt;form action="/signup" method="post"&gt;
  &lt;label for="email"&gt;Рабочая почта&lt;/label&gt;
  &lt;input id="email" name="email" type="email" required autocomplete="email"&gt;
  &lt;button type="submit"&gt;Создать аккаунт&lt;/button&gt;
&lt;/form&gt;</pre>
<p><b>Label</b> (подпись) связывается с полем через одинаковые <code class="i">for</code> и <code class="i">id</code>. Нажатие на подпись переводит фокус в поле, а скринридер использует подпись как имя поля. Placeholder — пример или подсказка внутри поля; он исчезает при вводе и <b>не заменяет label</b>.</p>
<p>Атрибут <code class="i">name</code> — имя поля в отправляемых данных. Без него значение обычного input, как правило, не попадёт в набор данных формы. Атрибут <code class="i">value</code> — текущее значение; для checkbox и radio он определяет, какое значение отправить, если элемент выбран.</p>

<h5>Типы полей и браузерная валидация</h5>
<p><code class="i">type="email"</code>, <code class="i">type="number"</code>, <code class="i">type="date"</code>, <code class="i">type="search"</code> помогают выбрать клавиатуру на телефоне и предоставляют встроенную проверку или элемент управления. Они не заменяют серверную проверку: клиент может изменить запрос вручную.</p>
<pre class="code">&lt;input type="number" min="1" max="10" step="1" required&gt;
&lt;input type="password" minlength="12" autocomplete="new-password"&gt;
&lt;input type="file" accept="image/png,image/jpeg"&gt;</pre>
<p><code class="i">required</code> требует значение, <code class="i">minlength</code>/<code class="i">maxlength</code> ограничивают длину, <code class="i">min</code>/<code class="i">max</code>/<code class="i">step</code> задают числовые ограничения, <code class="i">pattern</code> принимает регулярное выражение. Это улучшает удобство, но не является границей безопасности.</p>
<p>При перехвате формы через JavaScript вызывайте <code class="i">event.preventDefault()</code> только если действительно заменяете стандартную отправку. Для своей проверки можно использовать <code class="i">form.reportValidity()</code> — браузер покажет встроенное сообщение и переведёт фокус к неверному полю. После ошибки API нужно связать сообщение об ошибке с конкретным полем и сделать его доступным, например через <code class="i">aria-describedby</code>.</p>

<h5>Checkbox, radio, select и группы</h5>
<p><b>Checkbox</b> — независимый выбор «включено/выключено»; несколько checkbox могут быть выбраны одновременно. <b>Radio</b> — один вариант из группы: одинаковый атрибут <code class="i">name</code> объединяет элементы. Варианты следует объединять через <code class="i">fieldset</code> с подписью <code class="i">legend</code>, чтобы было ясно, к какому вопросу они относятся.</p>
<p><code class="i">&lt;select&gt;</code> подходит для компактного выбора из известного списка. Кастомный выпадающий список не становится доступным автоматически: нужно реализовать клавиатурную навигацию, управление фокусом, объявление текущего выбора и закрытие по Escape. Поэтому сначала проверьте, подходит ли нативный control.</p>

<h5>Как браузер загружает script</h5>
<table>
<tr><th>Вариант</th><th>Загрузка</th><th>Выполнение</th><th>Когда уместен</th></tr>
<tr><td>Обычный <code class="i">&lt;script src&gt;</code></td><td>останавливает HTML-парсер</td><td>сразу после загрузки</td><td>редкий случай, когда код должен выполниться прямо в этой точке документа</td></tr>
<tr><td><code class="i">defer</code></td><td>параллельно с парсингом</td><td>после разбора HTML, в порядке подключения</td><td>скрипты приложения, которым нужен готовый DOM</td></tr>
<tr><td><code class="i">async</code></td><td>параллельно с парсингом</td><td>сразу по готовности, порядок не гарантирован</td><td>независимая аналитика или виджет</td></tr>
<tr><td><code class="i">type="module"</code></td><td>по умолчанию откладывается</td><td>после парсинга с учётом импортов</td><td>современные ES-модули</td></tr>
</table>
<p>Два слова, которые часто путают: <b>парсинг</b> — разбор HTML и построение DOM; <b>выполнение</b> — запуск кода JavaScript. Обычный скрипт может остановить первое, чтобы перед запуском скрипт мог изменить ещё не разобранный документ.</p>

<h5>Изображения и загрузка по требованию</h5>
<p>Всегда указывайте <code class="i">width</code> и <code class="i">height</code> картинки, даже если CSS делает её адаптивной. Браузер заранее резервирует место, и страница меньше прыгает при загрузке. <code class="i">alt</code> — текстовая замена изображения: опишите важную информацию; для чисто декоративной картинки используйте <code class="i">alt=""</code>, чтобы скринридер не читал лишнее.</p>
<pre class="code">&lt;img
  src="chart-800.webp"
  srcset="chart-400.webp 400w, chart-800.webp 800w, chart-1600.webp 1600w"
  sizes="(max-width: 600px) 100vw, 800px"
  width="800" height="450"
  alt="Выручка выросла с января по июнь"
&gt;</pre>
<p><code class="i">srcset</code> перечисляет варианты файла и их ширину, <code class="i">sizes</code> сообщает браузеру ожидаемую ширину изображения на странице. Браузер выбирает подходящий вариант с учётом экрана и плотности пикселей. Не ставьте <code class="i">loading="lazy"</code> на главную видимую картинку: откладывание её загрузки может ухудшить LCP. Lazy loading уместен для изображений ниже первого экрана.</p>

<h5>HTML-детали, которые часто проверяют</h5>
<ul>
<li><code class="i">&lt;details&gt;&lt;summary&gt;…&lt;/summary&gt;…&lt;/details&gt;</code> создаёт нативный раскрываемый блок.</li>
<li><code class="i">&lt;dialog&gt;</code> вместе с методом <code class="i">showModal()</code> открывает модальный диалог в верхнем слое браузера; нужно дать ему понятный заголовок и закрытие клавишей Escape.</li>
<li><code class="i">&lt;picture&gt;</code> помогает выбрать формат или кадрирование изображения под условие; сам элемент не заменяет alt на <code class="i">img</code>.</li>
<li><code class="i">&lt;template&gt;</code> хранит разметку, которая не отображается и не исполняется до клонирования скриптом.</li>
<li><code class="i">&lt;iframe sandbox&gt;</code> ограничивает возможности встроенного документа; выдавайте только те разрешения <code class="i">allow-*</code>, которые действительно нужны.</li>
</ul>

<h5>Частые ошибки и короткий ответ</h5>
<ul>
<li>Кликабельный <code class="i">div</code> вместо кнопки или ссылки.</li>
<li>Placeholder вместо подписи, ошибка без текстового объяснения, инструкция только через цвет.</li>
<li>Контрол без <code class="i">name</code>; кнопка внутри формы без явного типа может неожиданно отправить форму.</li>
<li>Доверие клиентской валидации как защите от неверного или вредоносного ввода.</li>
<li>Большая картинка без размеров или lazy loading у главного изображения, которое нужно показать первым.</li>
</ul>
<div class="key">«Я выбираю семантический элемент по его назначению: ссылка меняет адрес, кнопка выполняет действие, форма связывает поля и отправку. Тогда браузер сам даёт базовую клавиатурную поддержку и семантику. Для кастомного поведения я отдельно сохраняю имя, фокус, состояние и доступное сообщение об ошибке».</div>

<h5>Термины главы</h5>
<ul>
<li><b>Семантика</b> — значение элемента и действие, которые браузер и вспомогательные технологии могут понять.</li>
<li><b>Скринридер</b> — программа, озвучивающая содержимое и состояние интерфейса.</li>
<li><b>Парсер</b> — программа, которая разбирает текст HTML и строит DOM.</li>
<li><b>Верхний слой (top layer)</b> — специальная область браузера над обычной CSS-раскладкой, где показываются модальные диалоги и некоторые popover.</li>
<li><b>LCP</b> — метрика скорости, измеряющая появление самого крупного видимого содержимого; связь с другими метриками описана в главе о производительности.</li>
</ul>`
}
