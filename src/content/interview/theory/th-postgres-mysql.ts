import type { TheoryArticle } from '@/engine/types'

export const postgresMysql: TheoryArticle = { id:'th-postgres-mysql', topic:'Базы данных', title:'PostgreSQL и MySQL: SQL, JOIN, транзакции и устройство хранения',
  lead:'Как проектировать реляционные данные, читать JOIN и планы запросов, понимать MVCC/блокировки и различия PostgreSQL с MySQL/InnoDB.',
  body:`
<h5>Реляционная БД — это не просто набор таблиц</h5>
<p>Реляционная модель описывает данные отношениями (таблицами), строками и столбцами; связь выражается ключами и ограничениями. <b>Primary key</b> идентифицирует строку, <b>foreign key</b> защищает ссылочную целостность, <b>unique/check/not null</b> закрепляют инварианты на уровне БД. Проверка только в приложении не защищает от гонок, второго сервиса, миграционного скрипта или ручной операции.</p>
<p>Нормализация уменьшает повторение фактов и аномалии обновления. Денормализация иногда ускоряет конкретное чтение, но создаёт ответственность за обновление копий и согласованность. Начинайте с корректной модели, затем денормализуйте по измеренной нагрузке и определите, как обновляется проекция.</p>

<h5>JOIN: что происходит с количеством строк</h5>
<p>JOIN комбинирует строки по предикату. Самая важная практика — до написания запроса определить кардинальность отношений: одна строка к одной, одна ко многим или многие ко многим. Если у заказа 4 позиции и 3 тега, два JOIN к коллекциям могут дать 12 строк заказа до группировки. Это не ошибка SQL, а произведение совпавших строк.</p>
<table><tr><th>Оператор</th><th>Смысл</th></tr>
<tr><td>INNER JOIN</td><td>оставить только пары, для которых условие истинно</td></tr>
<tr><td>LEFT JOIN</td><td>сохранить каждую левую строку; если пары нет, столбцы правой стороны NULL</td></tr>
<tr><td>FULL OUTER JOIN</td><td>сохранить строки обеих сторон, включая непарные</td></tr>
<tr><td>CROSS JOIN</td><td>каждую левую строку соединить с каждой правой</td></tr>
<tr><td>SELF JOIN</td><td>соединить таблицу с ней самой, например сотрудник — менеджер</td></tr></table>
<pre class="code">-- сохранить и клиентов без заказов, и посчитать только оплаченные заказы
SELECT c.id, COUNT(o.id) AS paid_orders
FROM customers c
LEFT JOIN orders o
  ON o.customer_id = c.id AND o.status = 'paid'
GROUP BY c.id;

-- если перенести o.status в WHERE, NULL-строки исчезнут
-- и запрос фактически станет INNER JOIN для этого условия</pre>
<p>Условие <code class="i">ON</code> определяет, какие строки считаются парой; <code class="i">WHERE</code> фильтрует итоговые строки. Условие правой таблицы в WHERE после LEFT JOIN отбрасывает строки, где справа NULL. Иногда это намеренно, но часто это незамеченная смена семантики.</p>

<h5>JOIN, EXISTS и агрегации</h5>
<p>Если нужно только проверить наличие связанной строки, применяйте <code class="i">EXISTS</code>: он выражает semi-join и не размножает родительскую строку на каждое совпадение. Для «найти родителей без детей» используйте <code class="i">NOT EXISTS</code>. <code class="i">NOT IN</code> неожиданно ведёт себя при NULL внутри подзапроса из-за трёхзначной логики SQL.</p>
<pre class="code">-- пользователи, у которых есть оплаченный заказ
SELECT u.id
FROM users u
WHERE EXISTS (
  SELECT 1 FROM orders o
  WHERE o.user_id = u.id AND o.status = 'paid'
);

-- пользователи без единого заказа
SELECT u.id FROM users u
WHERE NOT EXISTS (SELECT 1 FROM orders o WHERE o.user_id = u.id);</pre>
<p>Перед JOIN нескольких коллекций посчитайте ожидаемую кратность и агрегируйте дочерние таблицы отдельно, если иначе получается join explosion. <code class="i">COUNT(*)</code> считает итоговые строки, <code class="i">COUNT(child.id)</code> игнорирует NULL и поэтому полезен для LEFT JOIN. Агрегация по неуникальному полю имени может объединить разных людей — группируйте по ключу сущности.</p>

<h5>Запрос SQL: синтаксис и логический порядок</h5>
<p>Для рассуждения порядок примерно такой: FROM/JOIN → WHERE → GROUP BY → HAVING → SELECT → DISTINCT → ORDER BY → LIMIT. Реальный физический план оптимизатор может выполнять иначе, сохраняя результат по правилам SQL. <code class="i">WHERE</code> фильтрует строки до группировки, <code class="i">HAVING</code> — группы. Сортировка не гарантируется без ORDER BY, даже если данные «обычно» приходят в порядке вставки.</p>
<p><code class="i">LIMIT/OFFSET</code> без стабильного уникального ORDER BY может пропускать или повторять строки при изменениях. Большой OFFSET всё равно заставляет пройти пропущенные строки. Keyset pagination сравнивает last-seen tuple (например, created_at + id) и обычно лучше масштабируется, но требует корректного стабильного порядка.</p>

<h5>Индексы: ускорение поиска и цена записи</h5>
<p>Индекс — отдельная структура, поддерживаемая при изменениях таблицы. B-tree хорошо подходит для равенства, диапазонов, сортировки и префиксных условий. Индекс не «ускоряет запрос автоматически»: планировщик сравнивает стоимость index scan с sequential/table scan, учитывает статистику и число строк, которые придётся прочитать.</p>
<p>Составной B-tree индекс особенно полезен, когда запрос ограничивает ведущие столбцы. Для <code class="i">(tenant_id, status, created_at)</code> естественны запросы по tenant и дальше status/диапазону даты. Правило левого префикса — полезная эвристика, но не универсальное утверждение о том, что остальные колонки вообще не участвуют: конкретное использование зависит от движка, выражения и плана.</p>
<ul><li>PostgreSQL: B-tree, GIN (например JSONB/полнотекст/массивы), GiST (пространственные и обобщённые структуры), BRIN (большие коррелированные по физическому порядку таблицы), partial/expression indexes.</li><li>MySQL/InnoDB: clustered index по primary key; лист вторичного индекса содержит primary key. Длинный primary key увеличивает все secondary indexes.</li><li>Индексы на низкоселективном поле иногда полезны в составном/частичном виде, но могут проиграть сканированию — проверяйте план.</li><li>Функция над колонкой может мешать обычному индексу; помогают expression/generated-column индексы, если выражение и движок поддерживают это.</li><li>Не создавайте индексы «на всякий случай»: каждый занимает диск, влияет на cache locality и замедляет INSERT/UPDATE/DELETE.</li></ul>

<h5>EXPLAIN и поиск узкого места</h5>
<p>Сначала найдите реальные частые запросы и latency, затем изучайте план: estimated rows, actual rows, loops, выбранный join algorithm, сортировки, чтение страниц и spill во временные файлы. В PostgreSQL <code class="i">EXPLAIN (ANALYZE, BUFFERS)</code> реально исполняет запрос; аккуратно используйте его на production для дорогих/изменяющих данных запросов. В MySQL доступны <code class="i">EXPLAIN</code> и <code class="i">EXPLAIN ANALYZE</code> в поддерживаемых версиях.</p>
<p>Расхождение оценённых и фактических строк часто объясняет плохой план: статистика устарела, данные скошены, столбцы коррелируют или предикат не описан планировщику. Один Seq Scan не является автоматически багом — последовательное чтение маленькой таблицы может быть самым быстрым. Смотрите число повторных запросов, rows × loops, блокировки, сетевой round-trip и передачу лишних строк в приложение.</p>

<h5>Транзакции и ACID</h5>
<p><b>Atomicity</b> — все изменения операции вместе commit/rollback; <b>Consistency</b> — транзакция сохраняет заданные инварианты; <b>Isolation</b> — параллельные операции не должны наблюдать недопустимые промежуточные состояния; <b>Durability</b> — committed результат переживает предусмотренные отказы. ACID не означает, что база угадает бизнес-инвариант: его нужно выразить constraints, атомарным SQL, блокировкой или изоляцией.</p>
<p>Пример lost update: два запроса читают 10 мест, каждый видит свободное место и оба продают последнее. Безопасный вариант — условный атомарный UPDATE с проверкой количества изменённых строк либо транзакция с блокировкой строки/подходящей изоляцией.</p>
<pre class="code">UPDATE inventory
SET available = available - 1
WHERE sku = 'A-1' AND available &gt; 0;
-- успех только если affected rows = 1</pre>

<h5>MVCC, снимки и уровни изоляции</h5>
<p><b>MVCC</b> (multi-version concurrency control) позволяет читателям видеть согласованную версию строк, пока писатели создают новые версии; это часто уменьшает конфликт чтения с записью, но не устраняет все блокировки. Чтение собственного snapshot и блокирующий SELECT FOR UPDATE — разные режимы и имеют разные правила.</p>
<table><tr><th>Аномалия</th><th>Что происходит</th></tr>
<tr><td>Dirty read</td><td>транзакция увидела незакоммиченные данные другой транзакции</td></tr>
<tr><td>Non-repeatable read</td><td>повторное чтение той же строки увидело её изменённое committed значение</td></tr>
<tr><td>Phantom</td><td>повторный запрос по предикату вернул новые/исчезнувшие строки</td></tr>
<tr><td>Write skew</td><td>транзакции читают общий инвариант и меняют разные строки так, что вместе нарушают правило</td></tr>
<tr><td>Lost update</td><td>одно изменение затёрло параллельное обновление</td></tr></table>
<p>В PostgreSQL READ UNCOMMITTED реализуется как READ COMMITTED; стандартный default — READ COMMITTED, каждый statement получает новый snapshot. PostgreSQL REPEATABLE READ использует snapshot isolation и не показывает обычные phantom reads внутри транзакции, но может допустить write skew; SERIALIZABLE использует SSI и может завершить транзакцию serialization failure — приложению нужен ограниченный безопасный retry.</p>
<p>InnoDB по умолчанию использует REPEATABLE READ; обычные consistent reads видят snapshot транзакции, а locking reads при некоторых range-предикатах берут next-key/gap locks для контроля phantom вставок. В MySQL поведение отличается по типу чтения и движку/настройкам. Называйте конкретную БД и режим, а не только табличку из стандарта SQL.</p>

<h5>Блокировки, дедлоки и транзакционная дисциплина</h5>
<p><code class="i">SELECT ... FOR UPDATE</code> блокирует выбранные строки для конфликтующих операций до конца транзакции. Она не блокирует «всю таблицу» автоматически, но диапазон и тип индекса могут влиять на блокировки, особенно в InnoDB. Блокировки держите коротко, одинаково упорядочивайте захват нескольких ресурсов и задавайте deadline/lock timeout.</p>
<p><b>Deadlock</b> — цикл ожидания: T1 держит A и ждёт B, T2 держит B и ждёт A. База обнаруживает цикл и отменяет одну транзакцию. Это нормальный возможный исход конкурентности; код классифицирует ошибку и повторяет всю транзакцию ограниченно, если операции безопасны. Не делайте HTTP-вызовы, отправку почты и длительное ожидание пользователя внутри DB transaction.</p>

<h5>PostgreSQL: особенности, которые часто спрашивают</h5>
<ul><li>Основной движок хранит новые версии строк для MVCC; autovacuum освобождает место для повторного использования и поддерживает статистику. Длинная транзакция может удерживать старые версии и мешать очистке, вызывая bloat.</li><li>WAL (write-ahead log) сначала фиксирует изменения в журнале для восстановления/репликации; checkpoint переносит состояние на storage. fsync/synchronous_commit задают часть durability trade-off.</li><li>Внешний ключ требует подходящего индекса для частого поиска дочерних строк; PostgreSQL автоматически индексирует PK/UNIQUE, но не всегда индексирует referencing FK столбец.</li><li>Для больших таблиц индекс можно создавать <code class="i">CONCURRENTLY</code> (не внутри обычного transaction block, операция имеет собственные этапы и сбои); FK можно добавить NOT VALID и отдельно VALIDATE.</li><li>JSONB — удобный тип данных и операторов/GIN индексов, но он не отменяет схему и ограничения реляционной модели.</li></ul>

<h5>MySQL/InnoDB: важные отличия</h5>
<ul><li>InnoDB использует clustered index: строки таблицы организованы по primary key. Стабильный компактный PK важен; вторичные индексы содержат его значение.</li><li>Redo log поддерживает восстановление committed изменений; undo данные нужны для rollback и старых MVCC snapshots. Длинные транзакции создают нагрузку и задерживают очистку undo.</li><li>REPEATABLE READ — default распространённых конфигураций InnoDB; gap/next-key locking зависит от изоляции, индекса и типа запроса. Range-предикат может блокировать вставки в интервал.</li><li>Явно выбирайте <code class="i">utf8mb4</code> для Unicode, понимайте collation/case/accent правила сравнения, проверяйте timezone и SQL mode.</li><li>ALTER TABLE может быть instant/in-place/copy в зависимости от операции и версии; перед production миграцией проверяйте реальную стоимость и блокировки.</li></ul>

<h5>Репликация, чтение и согласованность</h5>
<p>Primary принимает запись и передаёт журнал репликам. Async replica может отставать: пользователь записал строку, затем сразу читает с реплики и не видит своё изменение. Это <b>read-after-write inconsistency</b>. Маршрутизируйте критичные чтения на primary, используйте session stickiness/LSN wait или проектируйте UI как eventually consistent. Реплика не является backup: ошибочное DELETE реплицируется тоже.</p>
<p>Синхронная репликация уменьшает окно потери данных, но добавляет latency и зависимость от доступности реплики. RPO (допустимая потеря данных) и RTO (время восстановления) задаются вместе с бизнесом, а не выводятся из факта наличия реплики.</p>

<h5>Транзакционная миграция схемы</h5>
<p>При rolling deploy одновременно работают версии приложения с разными ожиданиями схемы. Используйте expand-contract: добавить обратимо совместимое поле/индекс, выкатить код, backfill порциями, переключить чтение/запись, позже удалить старое. Большой backfill ограничивают по скорости и наблюдают по lag, блокировкам и нагрузке. Rollback кода может потребовать схему, которая ещё содержит и старые, и новые поля.</p>

<h5>Практика и вопросы интервью</h5>
<ol><li>Напишите LEFT JOIN всех пользователей и числа оплаченных заказов, включая ноль; сравните условие статуса в ON и WHERE.</li><li>Для «пользователи без заказов» сравните NOT EXISTS и NOT IN с подзапросом, содержащим NULL.</li><li>Объясните, почему два JOIN к коллекциям умножили строки, и перепишите через отдельную агрегацию/подзапрос.</li><li>Разберите lost update и выберите между atomic UPDATE, FOR UPDATE, optimistic version и SERIALIZABLE.</li><li>Прочитайте EXPLAIN ANALYZE: найдите неверную оценку строк, nested loop с большим числом loops и sort spill.</li><li>Спроектируйте keyset pagination для одинаковых timestamp, добавив уникальный tie-breaker.</li><li>Объясните, почему Redis lock не должен быть заменой ограничению UNIQUE или транзакции для платежа.</li></ol>
<p>Сильный ответ на выбор PostgreSQL/MySQL связывает требования с измеримыми особенностями: нужные типы/индексы, экосистема, операционные навыки, репликация, совместимость SQL и профиль нагрузки. Для обычного веб-продукта оба движка способны закрыть широкий набор задач; важнее правильно моделировать данные и эксплуатировать конкретную версию.</p>` }
