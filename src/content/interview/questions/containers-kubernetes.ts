import type { Question } from '@/engine/types'

export const containersKubernetesQuestions: Question[] = [
  { id:'container-process-vm', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'theory', level:'junior', q:'Почему Docker контейнер не является маленькой виртуальной машиной? Какие механизмы дают ему изоляцию и какие риски остаются?', answer:'<p>Контейнер — процесс(ы), разделяющий kernel host, а не отдельная guest OS/kernel. Namespaces ограничивают видимость ресурсов, cgroups контролируют потребление, capabilities/seccomp уменьшают полномочия. Ошибка kernel, privileged container, docker socket mount или опасные host mounts могут пробить ожидаемую границу; для hostile multi-tenant workload может требоваться более сильная isolation.</p>' },
  { id:'container-image-vs-runtime', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'theory', level:'middle', q:'Разведите понятия image, container, registry, runtime, OCI и Kubernetes CRI.', answer:'<p>Image — неизменяемое описание filesystem/config; container — запущенный процесс с image и runtime state. Registry хранит и раздаёт images. OCI определяет совместимые image/runtime форматы. Runtime запускает контейнерный процесс. CRI — интерфейс, через который kubelet общается с runtime (например containerd/CRI-O); Kubernetes не обязан запускать Docker Engine.</p>' },
  { id:'docker-layer-cache', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'manual', level:'middle', q:'Каждое изменение одного JS файла заставляет Docker заново устанавливать все зависимости. Как переставить слои и что внести в .dockerignore?', solution:'Сначала COPY package manifest и lockfile, отдельно выполнить frozen install, после этого COPY source и build; исключить node_modules, .git, dist, локальные cache, .env/secrets из build context. Проверить зависимости native modules/base OS при runtime и не полагаться на cache для reproducibility.', answer:'<p>Изменения source не должны инвалидировать dependency install layer. Скопировать package.json/lockfile, установить зависимости frozen, затем копировать исходники и собирать. .dockerignore исключает node_modules, .git, локальные dist/cache, секреты и ненужные документы. Multi-stage отделяет toolchain от runtime; обязательно перенести именно production dependencies и совместимые native modules. Cache ускоряет, но результат должен воспроизводиться из lockfile и pinned base.</p>' },
  { id:'docker-expose-port', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'theory', level:'junior', q:'Чем EXPOSE в Dockerfile отличается от `docker run -p`? Почему publish port может неожиданно открыть сервис наружу?', answer:'<p>EXPOSE — metadata/documentation о предполагаемом container port; оно само не публикует порт. -p создаёт host→container mapping. Bind address host-стороны важен: публикация без ограничения часто доступна на внешних интерфейсах, если firewall разрешает. Для внутреннего dev сервиса можно bind к 127.0.0.1; для production предпочтительно private network за reverse proxy.</p>' },
  { id:'compose-depends-ready', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'theory', level:'middle', q:'Почему depends_on не гарантирует, что база уже готова принимать запросы? Как сделать старт устойчивым?', answer:'<p>Порядок запуска контейнера не означает готовность приложения внутри него; процесс может стартовать раньше init/recovery/listener. Использовать healthcheck/condition там, где поддерживается, и обязательно retry/backoff/deadline в клиенте приложения. Операции миграции делать контролируемо отдельной задачей. Проверка готовности должна измерять реальную способность обслужить нужный запрос.</p>' },
  { id:'k8s-reconcile', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'theory', level:'middle', q:'Что значит desired state и reconciliation loop в Kubernetes? Почему self-healing не возвращает потерянные данные?', answer:'<p>API хранит желаемое состояние, controllers сравнивают его с наблюдаемым и непрерывно создают изменения к заданному виду, например заменяют удалённый Deployment Pod. Это восстанавливает декларативные объекты, но ephemeral filesystem исчезает, а некорректное desired config reproduces ошибку. Сохранность данных требует volume, backups и собственной модели восстановления.</p>' },
  { id:'k8s-pod-deployment', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'theory', level:'junior', q:'Что такое Pod и почему приложение обычно задают через Deployment, а не создают одиночный Pod вручную?', answer:'<p>Pod — минимальная scheduling единица из одного или нескольких связанных контейнеров с общей сетью/volume. Он эфемерен и может быть заменён с новым IP. Deployment через ReplicaSet поддерживает replicas и управляет rolling updates/rollback шаблона. Одиночный Pod не имеет контроллера для поддержания нужного количества и обычно не подходит долгоживущему приложению.</p>' },
  { id:'k8s-service-selector', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'manual', level:'middle', q:'Pods Running, Service существует, но в клиенте connection reset. Объясните роль selector, EndpointSlice, readiness, port и targetPort.', solution:'Проверить selector Service против labels Pod; EndpointSlices для ready адресов; readiness probe; Service port→targetPort mapping и protocol; listener container address. После этого NetworkPolicy, CNI, mesh и ingress. Running само по себе не включает Pod в endpoints.', answer:'<p>Service выбирает Pods selector-ом, а EndpointSlices показывают актуальные адреса/endpoints; неготовый Pod может отсутствовать. Проверить labels, selector, readiness, port/targetPort/protocol и listener внутри контейнера. Затем сверить NetworkPolicy, CNI/mesh и route. Running означает, что контейнерный процесс существует, не что endpoint готов и обслуживает правильный порт.</p>' },
  { id:'k8s-probes-separation', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'theory', level:'middle', q:'Как отличить startup, readiness и liveness probe и привести пример ошибочного дизайна каждой?', answer:'<p>Startup даёт приложению время первичной инициализации; readiness определяет включение в service traffic; liveness решает перезапуск зависшего процесса. Ошибки: слишком строгий startup вызывает restart до завершения миграции; readiness всегда 200 до загрузки config отправляет трафик рано; liveness зависит от DB и вызывает restart storm при внешнем сбое. Timeouts и thresholds должны соответствовать измеренному запуску/latency.</p>' },
  { id:'k8s-request-limit', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'theory', level:'senior', q:'Что делают requests и limits для CPU и памяти, и почему неправильные настройки могут ухудшить масштабирование?', answer:'<p>Requests влияют на scheduling и часто используются HPA CPU utilization denominator; слишком низкий request даёт плохой placement/scale signal, слишком высокий оставляет кластер недоиспользованным и Pod Pending. CPU limit способен вызвать throttling, memory limit — OOM kill. HPA увеличение replicas повышает нагрузку на DB/pool и требует node capacity. Подбирать по наблюдаемым профилям и SLO.</p>' },
  { id:'k8s-image-tag', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'theory', level:'middle', q:'Почему production deploy предпочтительнее фиксировать image digest, а не изменяемый тег `latest`?', answer:'<p>Тег может указывать на разные bytes во времени, нарушая reproducibility и усложняя rollback/аудит. Digest content-addressed и закрепляет точный image. CI связывает digest с commit, SBOM/signature/provenance; deploy проверяет доверие к artifact. Immutable tags тоже полезны, но digest остаётся точной идентичностью.</p>' },
  { id:'k8s-ingress-controller', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'theory', level:'middle', q:'Почему создание объекта Ingress само по себе не делает приложение доступным из Интернета?', answer:'<p>Ingress — декларативные HTTP routing правила. Нужен работающий Ingress controller/Gateway implementation, внешний LoadBalancer/IP/DNS, route до controller, корректный Service/Endpoints, TLS secret и разрешённая сеть/firewall. API объект не запускает proxy самостоятельно; события controller показывают reconciliation результат.</p>' },
  { id:'k8s-secret-base64', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'theory', level:'middle', q:'Почему Kubernetes Secret в YAML с base64 не следует считать зашифрованным секретом?', answer:'<p>Base64 — encoding, не encryption. Доступ ограничивается Kubernetes API/RBAC, но нужен encryption at rest для etcd, строгий access scope, rotation, audit и желательно внешний secret manager. Не хранить plaintext Secret в Git; GitOps требует encryption/sealed/external reference механизма.</p>' },
  { id:'k8s-pending-debug', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'manual', level:'middle', q:'Deployment создал Pod, но он Pending. Как диагностировать без бесцельного увеличения числа nodes?', solution:'kubectl describe pod и Events; проверить requests vs allocatable, quotas, taints/tolerations, affinity/nodeSelector, PVC/StorageClass binding, image architecture constraints и node conditions. Добавлять capacity только если подтверждена нехватка ресурсов, иначе исправить constraint/claim.', answer:'<p>kubectl describe pod показывает scheduler Events и конкретную причину. Проверить requests/capacity и allocatable на nodes; namespace quota; taints/tolerations, affinity и nodeSelector; PVC binding/StorageClass; node readiness и архитектуру. Scale nodes только если доказан дефицит capacity. PVC Pending или несовместимая affinity от масштабирования не исчезнут.</p>' },
  { id:'k8s-crash-loop', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'manual', level:'middle', q:'Pod попал в CrashLoopBackOff после rollout. Опишите порядок evidence-first диагностики.', solution:'kubectl logs --previous и describe/events; termination exit code/OOMKilled/probe failure; проверить command/args, image digest, config/secret mounts, user/permissions, dependency deadline и старый/новый release diff; сравнить replicas и rollback при impact.', answer:'<p>Сначала describe и events, затем logs --previous, termination reason/exit code и OOMKilled. Сравнить image digest, command/args, configmaps/secrets, permissions/working dir и probes с предыдущей версией. Проверить приложение в аналогичном container локально, dependency/timeout. Если impact растёт, остановить rollout или откатить к совместимому image, сохраняя evidence.</p>' },
  { id:'k8s-rbac-minimum', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'theory', level:'senior', q:'Как минимизировать Kubernetes privileges для приложения и CI deploy identity?', answer:'<p>Для runtime service account не монтировать token, если API не нужен; если нужен — выдать Role verbs/resources только необходимого namespace и использовать короткоживущую identity. CI deploy ограничить конкретными namespaces/resources/actions, отделить cluster infrastructure privileges, проверять audit logs и secrets. Избегать cluster-admin и privileged Pods по умолчанию.</p>' },
  { id:'k8s-rollout-db', theoryId:'th-containers-kubernetes', topic:'Docker и Kubernetes', type:'manual', level:'senior', q:'Новый deployment требует переименовать и удалить колонку БД. Почему обычный rollout и kubectl rollout undo не обеспечивают безопасный rollback?', solution:'Схема меняется независимо от Pod template; старые/new pods могут работать одновременно. Выполнить expand-contract: добавить совместимую колонку, dual-read/write/backfill, перевести приложение, наблюдать, отдельно удалить старое поле позже. Code rollback проверить на уже расширенной схеме; schema destructive шаг иметь restore plan.', answer:'<p>Rolling update допускает одновременную работу старых и новых Pods, а undo меняет только workload template, не возвращает данные/схему. Применить expand-contract: additive nullable schema, совместимый код, backfill, перевод чтения/записи, затем отдельный отложенный contraction после окна rollback. Для destructive migration нужен backup/restore и проверенная обратная совместимость.</p>' },
]
