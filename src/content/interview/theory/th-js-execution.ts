import type { TheoryArticle } from '@/engine/types'

export const jsExecution: TheoryArticle = {
  id: 'th-js-execution',
  topic: 'JavaScript',
  title: 'JavaScript: области видимости, замыкания, this и прототипы',
  lead: 'Что именно хранит окружение функции, откуда берётся this, как работает цепочка прототипов и почему равенство с приведением типов так легко ошибается.',
  body: `
<h5>Как JavaScript ищет переменную</h5>
<p><b>Область видимости</b> — участок кода, где имя переменной доступно. У <code class="i">let</code> и <code class="i">const</code> она блочная: имя доступно внутри ближайших фигурных скобок. У <code class="i">var</code> область видимости функции, а не блока.</p>
<pre class="code">function example() {
  if (true) {
    var old = 'видна во всей функции'
    let modern = 'видна только в этом блоке'
  }
  console.log(old)      // работает
  console.log(modern)   // ReferenceError
}</pre>
<p>Движок ищет имя в текущем окружении, затем поднимается к окружению внешней функции, и так далее — до глобального. Эту цепочку называют <b>лексической областью видимости</b>: она определяется тем, где функция написана в коде, а не тем, откуда её вызвали.</p>
<p>До строки объявления <code class="i">let</code>/<code class="i">const</code> имя находится во <b>временной мёртвой зоне</b> (TDZ): доступ к нему вызывает ошибку. Это не «переменная ещё не существует», а запрет читать или записывать её до объявления. <code class="i">var</code> поднимается и инициализируется значением <code class="i">undefined</code>; поэтому ошибка у <code class="i">let</code> помогает не использовать переменную раньше времени.</p>
<p><b>Поднятие объявлений</b> (hoisting) — удобное название для того, как движок создаёт привязки до выполнения строк тела функции. Это не значит, что физически весь код перемещается наверх.</p>
<pre class="code">say()                         // работает
function say() { console.log('готово') }

console.log(value)            // undefined
var value = 10

run()                         // TypeError: run не функция
var run = function () {}

read()                        // ReferenceError: TDZ
let read = () =&gt; {}</pre>
<p>Объявление функции доступно до своей строки в области видимости. У <code class="i">var</code> заранее существует привязка со значением <code class="i">undefined</code>, но присваивание происходит на месте. У <code class="i">let</code>/<code class="i">const</code> привязка до строки объявления находится в TDZ. Эти различия — причина предпочитать блоковые объявления и не полагаться на порядок, который читателю трудно заметить.</p>

<h5>Замыкание: функция помнит место, где была создана</h5>
<p><b>Замыкание</b> — функция вместе с доступом к внешним переменным, которые ей нужны. Функция может быть возвращена из другой функции и всё равно читать её локальную переменную: движок сохраняет окружение, пока оно нужно.</p>
<pre class="code">function makeCounter() {
  let value = 0
  return () =&gt; ++value
}

const next = makeCounter()
next() // 1
next() // 2 — локальная переменная живёт между вызовами</pre>
<p>Приватность тут не абсолютная — это просто недоступное снаружи имя. Замыкания используют для обработчиков, фабрик функций, сохранения состояния и конфигурации. Они могут удерживать память: если долгоживущий обработчик замкнул большой объект или DOM-дерево, объект останется достижимым, пока обработчик не удалён.</p>
<p>Классическая ловушка с циклом: <code class="i">var</code> создаёт одну переменную на функцию, и все callbacks могут увидеть её итоговое значение. <code class="i">let</code> создаёт отдельное привязанное к итерации значение.</p>
<pre class="code">const callbacks = []
for (let i = 0; i &lt; 3; i++) callbacks.push(() =&gt; i)
callbacks.map((fn) =&gt; fn()) // [0, 1, 2]</pre>

<h5>this задаётся вызовом, а не местом объявления</h5>
<p>У обычной функции значение <code class="i">this</code> определяется тем, <b>как её вызвали</b>. Четыре частых случая:</p>
<pre class="code">function show() { return this?.name }

show()                         // undefined в strict mode
user.show = show; user.show()  // this === user: вызов как метод
show.call(user)                // this передали явно
const fixed = show.bind(user)  // this зафиксирован навсегда
new Constructor()              // this — создаваемый объект</pre>
<p>У стрелочной функции собственного <code class="i">this</code> нет: она берёт его из окружающей функции при создании. Поэтому стрелка удобна в callback, которому нужен внешний объект, но не подходит как метод, которому нужен вызывающий объект, или как конструктор.</p>
<div class="warn">Если сохранить метод отдельно — <code class="i">const fn = user.show</code> — вызов <code class="i">fn()</code> теряет связь с <code class="i">user</code>. Передавайте <code class="i">user.show.bind(user)</code> или вызывайте через объект. DOM-обработчик и callback библиотеки могут задавать свои правила вызова.</div>

<h5>Прототипы: делегирование свойств</h5>
<p>Объект может иметь другой объект в качестве <b>прототипа</b>. Если свойство не найдено у самого объекта, JavaScript ищет его у прототипа, затем у прототипа прототипа. Эта цепочка заканчивается на <code class="i">null</code>.</p>
<pre class="code">const base = { describe() { return this.name } }
const item = Object.create(base)
item.name = 'Книга'

item.describe()              // 'Книга': метод найден в base, this остаётся item
Object.hasOwn(item, 'name')  // true
Object.hasOwn(item, 'describe') // false</pre>
<p><code class="i">Object.hasOwn</code> проверяет только собственное свойство; оператор <code class="i">in</code> ищет также по цепочке прототипов. Это важно при чтении пользовательского объекта и объектов вроде <code class="i">{}</code>, у которого есть унаследованные имена.</p>
<p>У функции-конструктора есть свойство <code class="i">prototype</code>: объект, который станет прототипом экземпляров, созданных через <code class="i">new</code>. У уже созданного объекта доступ к его прототипу можно получить через <code class="i">Object.getPrototypeOf(obj)</code>. Классы в JavaScript используют ту же систему прототипов, а не создают отдельный механизм наследования.</p>

<h5>Равенство и приведение типов</h5>
<ul>
<li><code class="i">===</code> не преобразует типы перед сравнением. Для примитивов сравниваются значения; для объектов — идентичность, то есть ссылка на один и тот же объект.</li>
<li><code class="i">Object.is</code> почти как <code class="i">===</code>, но считает <code class="i">NaN</code> равным самому себе и различает <code class="i">+0</code> и <code class="i">-0</code>.</li>
<li><code class="i">==</code> применяет правила приведения, которые зависят от типов. Некоторые специальные пары коротки, но полагаться на все правила сложно.</li>
</ul>
<pre class="code">{} === {}                    // false: два разных объекта
const a = {}; const b = a
a === b                      // true: один объект
Object.is(NaN, NaN)           // true
Object.is(0, -0)              // false
'' == 0                       // true: пустая строка приводится к 0</pre>
<p>Практическое правило — использовать <code class="i">===</code>. Исключение <code class="i">value == null</code> намеренно покрывает ровно <code class="i">null</code> и <code class="i">undefined</code>, но команда должна понимать и принять эту запись. Если важна «похожая структура» двух объектов, сравнение ссылок не поможет: нужна явная функция сравнения с правилами для вложенности, циклов, Date, Map и других типов.</p>

<h5>Преобразование в примитив и ловушки операторов</h5>
<p>Когда оператору нужно число или строка, объект проходит преобразование в примитив: язык ищет подходящий метод <code class="i">Symbol.toPrimitive</code>, <code class="i">valueOf</code> или <code class="i">toString</code>. У массивов <code class="i">toString()</code> склеивает элементы через запятую, а у обычного объекта обычно получается <code class="i">[object Object]</code>.</p>
<pre class="code">[] + []          // ''
[] + {}          // '[object Object]'
'5' - 2          // 3: минус требует число
'5' + 2          // '52': плюс склеивает, если появился string</pre>
<p>Такие выражения лучше не использовать как приём. В реальном коде приводите явно: <code class="i">Number(value)</code>, <code class="i">String(value)</code>, проверяйте <code class="i">Number.isFinite</code> и обозначайте допустимый формат данных.</p>

<h5>Исключения и границы ошибок</h5>
<p><code class="i">throw</code> прекращает текущий обычный ход выполнения и передаёт управление ближайшему подходящему <code class="i">catch</code>. <code class="i">finally</code> выполняется при успехе и при ошибке — даже если внутри <code class="i">try</code> был <code class="i">return</code>. Используйте <code class="i">Error</code> или его наследника, а не строки: у ошибки есть имя и stack trace (стек вызовов, показывающий цепочку функций).</p>
<pre class="code">class NotFoundError extends Error {
  constructor(id) {
    super('Запись ' + id + ' не найдена')
    this.name = 'NotFoundError'
  }
}

try {
  await loadUser(id)
} catch (error) {
  if (error instanceof NotFoundError) return showEmptyState()
  throw error // неизвестную ошибку не маскируем
}</pre>
<p><code class="i">try/catch</code> ловит синхронный throw и rejection, если вы <code class="i">await</code> промис внутри блока. Он не поймает позже брошенную ошибку из callback, который выполнился после завершения блока. Подробнее — в главе об асинхронности.</p>

<h5>Модули: import не равен копированию файла</h5>
<p><b>ES module</b> объявляет зависимости через <code class="i">import</code>/<code class="i">export</code>. Импортированное значение — live binding: при изменении экспортированной переменной модуль-потребитель увидит текущее значение, хотя сам не может присваивать ему. Модуль выполняется один раз на экземпляр модуля, а импорты образуют граф зависимостей.</p>
<p><b>CommonJS</b> использует <code class="i">require</code> и <code class="i">module.exports</code>, исторически распространён в Node.js. Между CommonJS и ESM есть правила совместимости, и смешение форматов может влиять на импорт default-экспорта и запуск пакета. <b>Tree shaking</b> — удаление неиспользуемых экспортов при сборке; надёжнее всего работает с ESM, когда зависимости можно анализировать статически.</p>

<h5>Термины главы</h5>
<ul>
<li><b>Лексическая область</b> — область, определённая вложенностью кода при написании.</li>
<li><b>Достижимость</b> — можно ли добраться до объекта из активных переменных и корней памяти; недостижимый объект может быть освобождён сборщиком мусора.</li>
<li><b>Идентичность объекта</b> — факт, что две переменные указывают на один и тот же объект.</li>
<li><b>Стек вызовов</b> — список функций, которые сейчас ожидают завершения; он помогает понять место возникновения ошибки.</li>
<li><b>ESM</b> — стандартная система модулей JavaScript с import/export.</li>
<li><b>CommonJS</b> — система модулей Node.js с require/module.exports.</li>
</ul>`
}
